# v0.8.7-dev 电脑端授权与角色统一

2026-09-23，用户明确授权三项默认勾选，并指出桌面角色与网页/Watch不同。

## 改动

- 前端三项授权默认true，连接成功后调用现有settings接口应用；后端仍保持未授权前关闭。记住用户取消勾选并保留即时撤销。不改变路由默认、不自动录音、不调用云服务。
- 旧桌面 CSS 使用横椭圆眼、绿边和米色卡片，与正式来源不一致；改用黑色圆体白色胶囊眼，网页与桌面共享Canvas渲染。
- 数据从正式固件资产快照导出27姿态/23序列，确定性转换工具同步两份目录和渲染器，支持--check且不计算哈希。保留桌面拖动/隐藏/控制台。
- 头部投影按固件公式；网页/桌面在相同状态下使用同一代码。桌面业务状态循环、眨眼调度、无IMU/触摸叠加，与设备不保证逐帧同步；不声称完整复刻原网站3D引擎。

## 验证

- Python75项、浏览器7项、桌面7项通过；新增默认勾选/连接应用、撤销重连保持、目录/渲染器一致性与27姿态有限坐标检查。
- `python tools/export_desktop_avatar.py --check`通过。
- 本地浏览器DOM确认三项checked，默认本地路由未变；网页角色截图与240×280桌面HTML预览检查通过，网页无console error。
- 未启动真实录音/云请求/新的BLE会话。未对当前用户活跃Electron强制退出；重启后的原生桌面透明效果仍需人工确认。
- 电脑源码v0.8.7-dev，设备仍v0.8.6-dev；无固件改动/编译/烧录，不更新旧v0.8.0发行包。

## 使用

网页刷新。桌面在托盘选择退出，再双击项目根目录start-gork-console.cmd，不能只关窗口（会收起到托盘）。旧发行包继续保留，不冒充本次更新。浏览器配置隔离，网页与Electron分别保存授权撤销选择。
