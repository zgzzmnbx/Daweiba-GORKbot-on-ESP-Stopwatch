# v0.11.0 语音服务迁移与验收

日期：2026-09-23。按用户“迁过来，不再跨项目文件夹找文件”执行，原目录保留，没有删除、移动来源或合并来源 Git 历史。

## 已完成

- Sol 导入 `03-Src/voice-service/` 源码/config/tests/依赖锁/托管入口，修改配置根目录解析，语音服务维持 0.3.2。
- Astra 在主项目创建 `Codex-Temp/voice-runtime/.venv/`，复制既有同版本解释器兼容的依赖包；不复制旧 Scripts 的绝对路径启动器，使用新环境 `python -m ...`。依赖 4162 文件、207319524 字节；逐文件相对路径/大小核对。
- 模型复制到主项目 `Codex-Temp/voice-runtime/models/`，390 文件、455896898 字节；清单核对通过。新运行目录无符号链接。没有复制私人录音、来源 Git、日志或历史发行包。
- 宿主 VoiceServiceManager 默认从当前根目录计算环境、托管脚本、cwd；本机旧配置已备份，移除外部项目绝对路径。显式 command=[] 仍支持只复用服务的隔离模式。默认子进程覆盖 VOICE_CONFIG 为当前模块配置，避免继承旧项目配置。
- Key 保留在已有 Windows 专用环境变量，不写入项目文件或日志。本地识别/云端朗读的页面默认及既有许可逻辑保持不变。

## 验证结果

| 范围 | 结果 |
| --- | --- |
| 新环境 import sherpa_onnx/numpy/fastapi/httpx/pytest | 通过，解释器路径属于当前项目 |
| 新环境 pip check | No broken requirements found |
| 迁入语音服务 pytest | 46 passed，2 项既有依赖弃用提示 |
| 控制台 + 五个工具 Python 回归 | 85 passed，含默认本地路径与显式禁用启动测试 |
| 真实本地 TTS | PCM16、单声道、16000 Hz、57690 帧，完整 WAV |
| 真实本地 ASR | 正确识别“你好，这是项目内语音服务的迁移测试。” |
| 云请求计数 | 测试前后均为 0 |
| 托管启动与复用 | 两次 ensure 复用同一自有 PID，100 本地音色/6 云端音色目录通过 |
| 退出 | stdin EOF 后服务 exit 0，模型子进程全部退出 |

测试端口 18875、18876 均为本次独立进程。模型/依赖复制清单、运行结果和日志在忽略的 `Codex-Temp/voice-runtime/` 与 `Codex-Temp/voice-migration-runtime-result.json`。首轮自检脚本误用 ASR 顶层 text 字段，修正为既有契约 result.text 后闭环通过，未修改服务协议。

## 启动状态与限制

正式端口 8765/8766 在启动前没有监听者。尝试启动正式 Electron 时被自动审批策略拒绝（仅返回 blocked by policy），没有绕过或替代启动；因此不声称正式实例已经运行。用户可双击根目录 `start-gork-console.cmd`，它将使用当前项目内的环境、源码、模型和配置。8878 仍为此前隔离界面预览，不代表正式语音服务。

本次没有云端付费调用、真人录音/听验、BLE 连接或固件写入。代码与本地模型测试通过，不代替实际麦克风、扬声器或设备验收。旧发行包不含新服务，使用源码启动器。

## 版本与恢复

电脑端 v0.11.0-dev，Electron 元数据 0.11.0；语音服务 0.3.2；固件 v0.8.6-dev。README、AGENTS、当前计划、PRD 20、CHANGELOG 同步。

限定修改前备份在 `Codex-Temp/voice-migration-before/`，包含旧本机配置、管理器、配置模板和验证脚本。本次本地 Git 仅存档新增语音模块/测试/PRD/验收及宿主差分；既有脏树和设备改动不批量提交。不发布、不推远端，不把忽略的环境/模型/配置作为代码提交。
