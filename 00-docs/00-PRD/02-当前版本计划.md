# 当前版本计划：电脑端源码 v0.13.0、8766 运行后端 v0.13.0-dev，固件源码及设备 app0 v0.9.0-dev

## v0.13.0 / 固件 v0.9.0：Opus 经 BLE 传输

目标：同一句完整短语从 PCM 改为 Opus 后明显缩短发送阶段；Watch 小人页仍清楚完整出声。新客户端用 HELLO 特定 transfer ID 请求第 5 字节能力标志；旧固件返回原 4 字节，自动退回 PCM。新固件在 `BEGIN_OPUS` 收取编码总字节、目标 PCM 采样数及预跳过采样数，逐包 ACK、COMMIT 后解码、PLAY。保持原 BLE 绑定/加密、10 秒上限、超时与取消；控制台显示编码格式和字节数。

已完成：本机源码/构建/自动测试、当前 Watch app0 写入和独立 `verify_flash`、同一 WAV 的 PCM/Opus 真机传输对照。PCM 178,944 字节/32.22 秒发送，Opus 18,748 字节/3.66 秒发送，两次设备均返回 `played`。正式对话页 TTS → Opus → Watch 两次收到 `played`，用户确认小人页完整念完且可闻、主观音质可接受。待完成：更细音色对照、动画/气泡、停止和重连。后续阶段再做边收边播及 TTS 流式输出；语音服务目前仍返回完整 WAV，传输提速不代表 TTS 已流式化。见 `04-output/v0.13.0/Opus实施与真机对照-【codex】.md`。

## v0.12.9 / 固件 v0.8.8：小人页朗读与 BLE 吞吐

旧版最近成功任务 57,600 字节、32.64 秒（含播放等待），旧电脑端每包都等待回执。v0.12.9 最多 3 包在途，进度仅按有效设备确认字节推进；设备端 4 包队列、CANCEL 和 COMMIT/PLAY 安全边界不变。按大尾巴当次授权，v0.8.8 仅写入 app0 并独立校验；普通小人页现可接收/播放音频，录音仍限 Audio test，动态/内置声音互斥。正式实例 v0.12.9 对完整短句两次真机播放，用户确认第二次清楚完整：约 89.6 KB 传输 17.5 秒，含设备播放约 18.5 秒。旧新样本不同，暂不计算严格 A/B 倍数。剩余：降低十余秒等待、检查动画帧率/文字气泡/停止/内置短音互斥、Audio test 录音和断线恢复。记录见 `04-output/v0.12.9/实施与验收记录-【codex】.md`。

## v0.12.8-dev：状态日志阅读优化（2026-09-24）

左侧状态日志改为单行工具栏、14px 正文与 196px 阅读区；点击展开切换到右侧“运行日志”标签页，支持全部/蓝牙/语音/异常筛选、复制当前结果、清空与自动跟随，向上翻阅暂停跟随。黑白主题同步，正常/成功/警告/失败分色，沿用 120 条内存摘要，不新增设备或语音请求。40 项前端回归通过；隔离网页验证展开、筛选、复制、主题及 1180×820、390×540 无整体溢出。正式实例未重启，本次未操作设备。详见 `04-output/v0.12.8/状态日志验收记录-【codex】.md`。


## v0.12.7：Watch 朗读失败诊断

正式 v0.12.6 实例有一次 Watch 朗读任务在建立音频会话阶段失败，0/69120 PCM 字节传输，但因后端空错误字符串被前端当成成功。现在终态须为 `played` 才能显示播放完成；超时信息标明 HELLO/BEGIN 等命令，并提示设备进入 Settings → Audio test。Watch 文字显示不构成音频传输证据。源码和自动回归已完成；正式进程未重启，进入 Audio test 后的设备朗读和听感待人工验证。固件未改、未烧录。

## v0.12.6：统一文字输入与输出

对话页一个文字框选择电脑朗读、小人显示（Watch/桌面/两端）或 Watch 朗读（延迟实验）；原文/AI 回答单独选择。角色页只保留气泡清除，Watch 页移除电脑文字朗读的重复输入，设备录音实验继续保留。Watch 显示在提交前校验 24 字/72 UTF-8 字节，不静默裁剪；设备朗读继续沿用音频 10 秒上限和停止/代际取消。源码与 50 Node、46 Python 回归及隔离浏览器布局通过；正式实例未重启，真实 Watch 显示、朗读与长稳另验。记录见 `04-output/v0.12.6/实施与验收记录-【codex】.md`。

## v0.12.5：桌面小人始终置顶

桌面小人窗口置顶由固定开启改为控制台可切换；旧用户状态默认保持开启。保存到既有窗口状态文件，重启恢复；网页端禁用该开关。桌面 IPC 与页面回归 45 项通过，正式 Electron 实屏与重启恢复待验收。不涉及语音、BLE 或固件。

## v0.12.4：状态终端与分栏优化

左侧增加黑白可切换只读终端，接入既有状态提示、语音/BLE变化、回执摘要和音频阶段；通道去重、120条上限、内存保存、清空与主题记忆。收紧左栏、加竖向分隔，保留原控制开关。46 Python + 43 Node 回归通过；隔离浏览器验证主题切换/刷新恢复/清空及1180×820、390×540布局。正式进程未重启，未改语音/设备行为、未烧录；详见 v0.12.4 验收记录。

## 当前 BLE 稳定连接门禁

电脑端在程序内为首次配对留足等待时间，保留唯一目标扫描和单 BleakClient，增加首次失败及掉线后的 5–30 秒退避重连；手动断开停止重试。固件 v0.8.7 修复绑定设备使用临时地址重连时可能在认证前被拒绝的问题，已仅写入 app0 并验证。旧配对状态下 WinRT GATT 发现仍失败；手表清绑定、电脑端程序化取消旧配对后，已在程序内重新配对并成功连接目标 GATT。继续验证命令回执、10 次断开重连、30 分钟稳定连接、陌生端拒绝和表情/声音基础功能；不能仅凭 Windows 蓝牙列表状态宣称稳定。保留旧 v0.8.6 恢复路径，见 `04-output/v0.8.7/蓝牙连接修复与验收记录-【codex】.md`。

## v0.12.2：开机自动连接语音

控制台加载后等待本机语音服务就绪，自动创建当前窗口的语音会话并应用已保存路由。自动连接不触发录音、播放、上传或会话接管；已有单活会话仍显示手动接管。服务较慢时后台重试，隔离预览可通过 `auto_connect_voice=false` 关闭。

验证口径：浏览器自动测试必须证明只创建一次 `/api/session`、应用一次 `/api/settings`，且没有 `/api/tts` 或录音入口；Python 测试验证配置布尔值和健康接口策略字段。固件、设备写入、真实云端请求和真人听验均不在本版本范围。

## v0.12.1：电脑双窗口动画同步

控制台左侧小人与桌面悬浮小人共用表情、播放模式和启动时间；自动语音表情与本地预览均同步。受限 Electron IPC 只接受控制台窗口，沿用 Watch 目标边界。桌面 15 项、控制台 22 项自动测试通过；正式双窗口实屏待重启验收，旧发行包与设备固件未更新。

## v0.12.0：桌面尺寸与启动蓝牙

本轮实施桌面 25%–150% 缩放、尺寸持久化、小尺寸退化和后台启动扫描连接。规格及验证见 `04-output/v0.12.0/实施与验收记录-【codex】.md`。未启动正式实例或连接实物；自动测试覆盖配对候选选择、手动断开优先级和 UI/IPC 尺寸边界。

## 当前：v0.11.0 语音项目内迁移

PRD 20 已实施。语音源码/模板/测试/入口与本机模型、独立环境已迁入，宿主默认指向当前项目。46 项语音测试、85 项控制台/工具测试和真实本地合成/识别闭环通过；关闭后推理子进程退出。没有云请求、录音硬件或设备操作。

自动启动正式 Electron 被审批策略拒绝，没有继续绕过。下一步用户双击 `start-gork-console.cmd` 进入正式控制台。旧目录保留、新目录已可独立加载；旧 v0.8.0 发行包不更新。结果见 `04-output/v0.11.0/迁移与验收记录-【codex】.md`。

以下是此前版本阶段记录。

## 当前：v0.10.0 单页自适应工作台

用户批准原型 v1.0.3 后，Astra 已完成正式界面，Sol 接手自动回归检查。布局以 PRD 19 为准：左侧机器人/状态/开关，右侧四标签；默认云端朗读、本地识别并保留旧偏好。沿用 PRD 18 独立语音边界。

- P0–P3：基线快照、视口布局、控制入口、对话和录音状态改造已实施。
- P4：中文角色目录、设备专属表情/文字长度校验、独立设备文字输入、设置草稿和即时撤销已实施。音色继续使用服务能力目录，不增加虚构音色。
- P5：自动回归与隔离网页验证见 v0.10.0 验收记录；正式 Electron、真人麦克风、云端/设备运行验收仍待执行。
- 旧安装包和运行实例不自动更新。范围内未改固件、未烧录、未发云请求、未搬迁语音服务。

以下为 v0.9.0 历史阶段记录，不代表当前未实施。

## 当前：v0.9.0 四页重组（源码与自动回归完成）

权威范围改为 [PRD 16](16-v0.9.0-Gork控制台界面重组PRD-【codex】.md) 和 [Goal 任务书 17](17-v0.9.0-Goal模式执行任务书-【codex】.md)，文档 v1.0.0，2026-09-23。对话、角色、StopWatch、设置四页；沿用当前风格，区分旧功能搬迁与角色控制/工作台控制权等必要补齐。

用户追加的语音模块化诉求见 [强制补充 18](18-v0.9.0-语音模块复用边界与接口验收-【codex】.md)：继续接口复用原独立服务，提取客户端音频模块、保留单活会话边界，提供不依赖 Gork 的 ASR/TTS 示例和 V01–V08 验收；并入 M2/M3/M5，尚未实施。

| 阶段 | 交付 | 状态 |
| --- | --- | --- |
| M0 | 代码与测试基线、范围/接口冻结、验收报告 | PASS |
| M1 | 四页骨架、共享状态、导航与全局停止入口 | PASS（源码/自动测试） |
| M2 | 对话流程、集中语音设置、许可/偏好迁移 | PASS（源码/自动测试） |
| M3 | 控制权解耦、角色播放/气泡/目标支持矩阵 | PASS（模拟/自动测试） |
| M4 | 设备日常与实验分层、桌面联动和停止链 | PASS（源码/自动测试） |
| M5 | 自动回归、实际 UI 检查、版本与文档交付 | IN_PROGRESS（待隔离浏览器截图与文档收口） |

本次电脑端源码为 v0.9.0-dev；独立语音服务仍 v0.3.2、设备仍 v0.8.6-dev。未改外部语音引擎、模型或密钥，未改固件/烧录、未发云端请求、未重启正式实例，也不更新旧发行包。M5 的浏览器截图和人工门禁按验收报告继续记录。

## 已有基线：v0.8.8 及历史实施

2026-09-23 新增语音设置和服务生命周期完善：能力驱动的音色选择、本地语速、固定文字试听、浏览器保存；云端服务新增逐请求选声（独立服务 v0.3.2）。后台自动启动、状态、重试和仅关闭自有实例；本机忽略配置已接入独立前台入口。验收覆盖旧服务兼容、上传撤销、试听取消、重复启动与模型子进程退出；实测报告见 `04-output/v0.8.8/语音设置与托管验收-【codex】.md`。不烧录、不发云请求、不搬模型/Key。

2026-09-23 电脑端补丁：用户授权三项默认勾选，连接时应用并保留持久化撤销；桌面与网页统一正式 Watch 来源数据和共用渲染器。75+7+7 自动测试、目录一致性及浏览器网页/240×280桌面外观预览通过。未烧录、未改语音服务、未发云请求；需退出旧 Electron 并从根目录启动器重开，旧发行包未更新。动画采用同源姿态/时间轴与固件投影，不含硬件IMU/触摸叠加，不能称与 Watch 逐帧完全相同。

2026-09-23 最新：用户确认已写入的 v0.8.5-dev 在 100% 音量仍不够大。内置声音 PCM16 峰值已为 30,000/32,767，不能再直接乘 3；v0.8.6-dev 改为只在六个内置短音播放时将 ES8311 REG32 从 `0xBF`（0 dB）提高到 `0xD2`（+9.5 dB，名义电压约 2.99 倍），动态语音和录音回放仍保持 0 dB，并在 Settings 首页显示版本号。75+5+5 项回归及固件构建通过；按当次授权仅写入 app0 应用区、独立读回通过，设备现为 v0.8.6-dev。用户随后真机确认声音达到正常大小，最大响度不足问题关闭；版本文字实屏、六种声音逐项音质、发热和长期稳定性仍待验收。

以下为历史 v0.8.0 阶段范围（PRD 13、Sol 任务书 14），不是下一轮授权。用户曾于 2026-09-21 启动实施并当次授权 v0.8.0-dev 应用区烧录；历史授权不自动扩展到后续设备写入、云端调用或 GitHub 推送。

| 阶段 | 内容 | 当前状态 |
| --- | --- | --- |
| M0 | 基线、依赖、协议、职责 | PASS：基线回归、Electron 锁定、声音协议与验收记录已落地 |
| M1 | 统一入口、桌面小人、托盘 | PASS（开发/本机烟测）：单实例、托盘、透明五状态角色、共享状态、权限/导航边界、位置恢复；托盘/多屏人工验收待做 |
| M2 | 电脑语音、服务托管、回答适配 | PASS（实现/模拟）：外部复用与自有启动、第三项独立授权、有界上下文/取消、未配置禁用；真实回答未配置未调用 |
| M3 | 设备内置声音、音量、遥控 | PARTIAL：v0.8.6 已写入，用户真机确认声音达到正常大小，最大响度不足问题关闭；六种声音逐项音质、静音/分档/停止和重启保持仍待验收 |
| M4 | 延迟音频、真实进度、取消 | PASS（实现/模拟）：同一 BleakClient、三种模式、真实字节/速率/阶段、取消与内存释放；真机三路径待测 |
| M5 | 回归、Windows交付、真机验收 | PARTIAL：75 Python + 5 浏览器 + 5 桌面测试通过，v0.8.6 构建、应用区写入和读回完成；A5/A6 部分通过，其余人工/真机项继续保留 |

## v0.7.2-P0 历史实施与持续门禁

最新听验覆盖一次短录音回放，不覆盖 P2 安全/长稳和全部硬件；下方“待录音复测/FAIL”为修复前或听验前快照。

最新烧录记录：2026-09-21 按用户当次授权，仅写 app0 `0x10000`，1,575,200 bytes；写入校验及独立读回通过，已发送硬复位。设备现在为 v0.7.2-P0；下方未烧录文字为开发历史。P1 仍待实际录音/回放，不能计作通过。

2026-09-21：用户实测 v0.7.0-P0 按黄色 A 后报 code 5，P1 录音失败。修正旧循环时间减新采集开始时间的下溢；细分 E9–E13，并保留错误提示。当前源码 v0.7.2-P0、电脑工具 v0.7.2；设备仍旧版。下一步取得当次烧录授权后复测录音字节增长、松开回放、B 停止及断连停止；不得仅凭构建关闭 P1。

## v0.7.0 纯 BLE 设备录放音（2026-09-21）

客户端补丁 v0.7.1：定向无缓存音频服务发现已在真机读到两个新特征；用户重开蓝牙后，两次连接的 HELLO/CAPS 和 PING 回执通过，协商包长 244 bytes，连接已释放。设备固件未变，不重烧、不清配对；18 项音频客户端模拟测试通过。实际录放、吞吐、安全与长稳仍待测，不关闭 P1/P2。

用户已批准开发纯蓝牙路线；已实现 P0 并通过构建及模拟测试。2026-09-21 按当次授权仅写入设备应用区 v0.7.0-P0，读回通过；现有语音工作台仍 v0.6.0。详见 PRD 11、协议/操作文档 12 和 `04-output/v0.7.0/实施与验收记录-【codex】.md`。

| 门禁 | 当前状态 | 下一步 |
| --- | --- | --- |
| P0 软件可行性 | 源码、编译、PC 模拟测试通过 | 不代表嵌入式运行、收音/出声、原固件全部行为回归 |
| G1 烧录授权与写入 | PASS | 当次明确授权；重认设备和分区，仅应用区写入与独立读回通过，不改分区/NVS |
| P1 设备录放 | FAIL（旧版录音 code 5）；补丁待烧录复测 | 低音量 tone、echo、录音电平/声音听验、按键/断开停止 |
| P2 BLE 吞吐/安全 | NOT RUN | 实测 credit=1 基线，再决定多包窗口；20 次取消和陌生端通知负测 |
| P3 语音闭环 | NOT IMPLEMENTED | 设备音频模式接入既有工作台和独立服务；不跳过 P1/P2 |
| P4 全量回归 | NOT RUN | 实屏、旧 BLE 表情/气泡、FPS、功耗/长稳 |

> 2026-09-20：按用户“先备份，再开发”实施。当前 Flash 两次完整读取一致；电脑端实现完成，无固件改动/烧录。首轮依据 `00-docs/02-交接/电脑语音交互首轮任务书-v1.0.0-【codex】.md`，新安排优先于下方历史计划。

## v0.6.0 当前门禁

| 任务 | 当前结果 | 验收边界 |
| --- | --- | --- |
| 开发前 Flash 恢复点 | PASS | 两次独立 16 MB 读取逐字节一致，应用区匹配 v0.5.0；仅本地保存，无文件 SHA 计算 |
| S1 HTTP 接口客户端 | PASS（模拟 + 本机接口） | 会话、选路、turn、ASR/TTS、取消、401/409；真实本地 TTS→ASR 完成，无云请求 |
| S2 BLE 客户端 | PASS（模拟）；实机待测 | 抽取共用模块，旧 9 项测试保留；匹配/迟到/丢回执/BUSY/串行/断开测试通过；本轮扫描未发现设备 |
| S3 跟读工作台 | 页面/本地回读 PASS | 录音代码、转写编辑、隐私开关、完整 WAV；真人麦克风录音/听感 NOT RUN |
| S4 状态联动 | PASS（模拟）；实屏待测 | 现有表情名映射，PC 播放器驱动 speaking/idle，不做嘴型；BLE 实屏 NOT RUN |
| S5 停止与恢复 | PASS（模拟 + 部分浏览器） | 20 轮迟到请求丢弃、20 次活动播放器停止、20 次后台取消；网页第二会话占用提示通过；真人中断听验待测 |
| S6 交付 | 源码、启动器、文档和测试已落地 | 45 项自动测试；本地 Git 阶段存档，不自动推送；无重新构建或烧录固件 |

详见 `10-v0.6.0-电脑语音工作台-【codex】.md`、`04-output/v0.6.0/实施与验收记录-【codex】.md`。旧固件门禁不因电脑端通过而关闭。

## v0.5.0 历史版本与未完成验收

> 固件源码与设备应用区仍为 v0.5.0；已写入并读回校验，运行效果待验收。v0.4.0 `happy-work` 和 v0.3.0 BLE 的未完成门禁继续保留在下方。

## v0.5.0 BLE 文字气泡门禁

| 门禁 | 状态 | 证据或停止条件 |
| --- | --- | --- |
| Windows 控制台 | PASS（模拟） | `:say` UTF-8 分包、`:clear`，事务回执匹配和错误处理；9 项客户端测试通过 |
| 固件协议与构建 | PASS（源码/本机） | 原加密 GATT 特征继续使用；主循环重组、UTF-8 检查；PlatformIO 构建成功，版本化镜像已归档 |
| 写入与视觉 | 写入 PASS；视觉 NOT RUN | COM5/MAC/ESP32-S3/16MB 核对；只写应用区 `0x10000` 并读回通过；实屏中文、两行、自动清除和无残影仍待测 |
| 性能与回退 | NOT RUN | 气泡开/关 FPS 与渲染耗时；BLE 表情、USB、本地按键和 Settings 回归 |

详见 `09-v0.5.0-BLE文字聊天气泡-【codex】.md` 与 `04-output/v0.5.0/实施与验收记录-【codex】.md`。

## v0.4.0 历史且尚未关闭的 happy-work 门禁

> 历史实施快照：当时源码为 v0.4.0，设备应用区为 v0.3.0；当前源码已升至 v0.5.0。v0.3.0 BLE 的未完成门禁继续保留在下方。

## v0.4.0 happy-work 门禁

| 门禁 | 状态 | 证据或停止条件 |
| --- | --- | --- |
| 派生表情实现 | PASS（源码） | 复用来源 `happy` 眼睛关键帧，纸笔/手部书写层仅在 `happy-work` 绘制，退出时清除旧图层 |
| 命令与回归 | PASS（静态/模拟） | USB、BLE 支持 `happy-work` 和显式播放前缀；17 项 Python 测试通过，原 23 条来源序列不改 |
| 固件构建 | PASS（本机） | PlatformIO 构建成功；版本化应用镜像见 `04-output/v0.4.0/firmware/` |
| 写入与视觉 | NOT RUN | 取得当次烧录确认、重新识别目标后仅写应用区；由大尾巴实物确认纸笔动作、清屏、自动回待机 |
| 性能 | NOT RUN | 真机记录 `happy-work` FPS、渲染耗时与无明显残影；构建成功不能替代实测 |

详见 `08-v0.4.0-happy-work派生表情-【codex】.md`。以下 v0.3.0 门禁仍然有效，不能因新版本构建而视为通过。

## v0.3.0 历史且尚未关闭的 BLE 门禁

> 历史实施快照：当时工程源码与设备应用区均为 v0.3.0（已写入并读回校验）；当前源码已升至 v0.5.0。
> 主题：使用本电脑内置蓝牙，通过 BLE GATT 无线控制 StopWatch 的 Grok bot 表情  
> 状态：BLE 固件、Windows 控制台、静态测试、G3 预检、应用区写入和 G4 核心链路已完成；剩余安全负向、压力和长期稳定性验收。  
> 权威任务书：`07-v0.3.0-BLE表情操控开发PRD-【codex】.md`

## v0.3.0 当前门禁

| 门禁 | 状态 | 证据或停止条件 |
| --- | --- | --- |
| G0 本机/固件基线 | PASS（本机范围） | `04-output/v0.3.0/evidence/L0-baseline-20260917.txt`；设备现有固件仍为 v0.2.5，既有脏工作树已保留 |
| G1 BLE 与安全可行性 | PASS（构建范围） | `04-output/v0.3.0/evidence/L1-ble-probe-build-20260917.txt`；实际 Windows 配对和安全负测仍留给 G4 |
| G2 固件/客户端/测试 | PASS（静态/模拟范围） | `04-output/v0.3.0/evidence/L2-L3-static-tests-20260917.txt`；5 项客户端测试通过，正式固件构建通过 |
| G3 写前报告 | PASS、已放行 | 重新枚举 COM5、核对 MAC/芯片/Flash、读取 app0 回退副本；已收到大尾巴“执行烧录”指令 |
| G4 无线真机 | 部分通过，继续收敛 | 配对、服务发现、状态通知、`ERR:BUSY`、23/23 表情、播放前缀和一次重连 PASS；陌生端拒绝、10 次重连、P95/FPS/稳定性/USB 回退待补测 |
| G5 交付 | 部分完成 | 固件构建产物、BLE 工具、测试证据、使用说明及项目状态文件已更新；真机证据缺失 |

详见新 PRD 与 `04-output/v0.3.0/实施与验收记录-【codex】.md`。以下 v0.2.6 与更早门禁均为历史记录。

## v0.2.6 历史门禁

> 版本主题：Windows 表情串口控制台  
> 当前状态：控制台已创建并通过 COM5/115200 真机发送测试；设备返回 `Command accepted: HAPPY`，固件仍为 v0.2.5。

### v0.2.6 原门禁

| 门禁 | 状态 | 证据或条件 |
| --- | --- | --- |
| 双击启动与端口识别 | PASS | `.cmd` 打开 PowerShell 控制台；自动识别当前 `VID_303A&PID_1001` 唯一串口 |
| 表情发送 | PASS | `happy` 带换行发送；设备回显 `Command accepted: HAPPY` |
| 边界 | PASS | 不改设备固件；仅允许已知表情与 `once/loop/pingpong` 前缀；`:q` 关闭串口 |
| 用户体验 | 待大尾巴确认 | 窗口已打开，可自行切换表情并观察屏幕 |

入口：`tools/start-expression-console.cmd`；使用说明：`tools/README.md`。

## v0.2.5 历史门禁

> 版本主题：调试模式表情名移至头像顶部  
> 当前状态：12 项现有测试和 PlatformIO 构建通过；已写入应用区并读回校验，顶部显示效果待大尾巴真机验收。  
> 已写固件：`04-output/v0.2.5/firmware/firmware-v0.2.5-grok-bot-debug-top.bin`

### v0.2.5 原门禁

| 门禁 | 状态 | 证据或条件 |
| --- | --- | --- |
| 顶部定位 | 本地实现 | 调试小白字中心位于圆屏顶部 y=42；文字尺寸与原逻辑不变 |
| 测试与构建 | PASS | 生成检查、12 项现有测试、PlatformIO 构建成功 |
| 应用区写入/读回 | PASS | 当次核对 MAC，仅写 `0x10000`；v0.2.5 读回 `verify OK` |
| 真机验收 | 待大尾巴验证 | 顶部字样可读、切换表情同步、关闭后消失；v0.2.4 的电量与亮度等门禁仍待验证 |

详细记录：`04-output/v0.2.5/写入与待验收记录-【codex】.md`。

## v0.2.4 历史门禁

> 版本主题：设置页电量显示与调试模式，合并待写的 v0.2.3 亮度调节  
> 当前状态：12 项 Python 测试和 PlatformIO 构建通过；v0.2.4 已写入应用区并读回校验通过，真机读数、字样和持久化待大尾巴验收。  
> 视觉方案：`06-v0.2.1-Grok-bot视觉对齐方案-【codex】.md`  
> 已写固件：`04-output/v0.2.4/firmware/firmware-v0.2.4-grok-bot-battery-debug.bin`，SHA-256 `CBCE21D83F01890AB649000EA5FE41806A146357C61F46EC122F48B53830EA4A`

### v0.2.4 原门禁

| 门禁 | 状态 | 证据或条件 |
| --- | --- | --- |
| 设置页结构 | 本地实现 | 电量、亮度、调试开关位于设置首页；原硬件检查保留为子页 |
| 电量显示 | 本地实现 | M5PM1 电压估算百分比/电压，5 秒刷新；读数无效显示 `--`，不宣称精确 mAh 容量 |
| 调试字样 | 本地实现 | 开关默认关、NVS 保存；打开后头像底部显示当前英文表情名，关闭后清除 |
| 数据与构建 | PASS | 生成检查、12 项 Python 测试、PlatformIO 构建成功 |
| 应用区写入/读回 | PASS | 当次核对设备身份、备份和 v0.2.2 应用，仅写 `0x10000`；v0.2.4 读回 `verify OK` |
| 真机验收 | 待执行 | 电量读数、亮度、调试字样、持久化、硬件检查及自动回待机实测 |

详细记录：`04-output/v0.2.4/电池与调试模式实施验收-【codex】.md`。

## v0.2.3 历史门禁

| 门禁 | 状态 | 证据或条件 |
| --- | --- | --- |
| 设置入口与交互 | 本地实现 | 长按 A+B 进入/退出 `Settings`；触屏 `−/+` 调节亮度，保留硬件检查 |
| 亮度边界与持久化 | 本地实现 | 默认 150、范围 30–255、步长 15；ESP32 NVS `gorkbot/brightness` 保存并启动读取 |
| 数据与构建 | PASS | 生成检查、12 项 Python 测试、PlatformIO 构建成功 |
| v0.2.3 独立固件写入/读回 | 未执行 | 亮度功能已并入已写入的 v0.2.4，不再单独烧录 v0.2.3 |
| 真机交互与重启恢复 | 待验收 | 点击 `−/+` 即时改变亮度，到边界不越界，重启后恢复；原诊断和表情功能不回退 |

详细记录：`04-output/v0.2.3/亮度设置实施与验收记录-【codex】.md`。

## v0.2.2 历史门禁

| 门禁 | 状态 | 证据或条件 |
| --- | --- | --- |
| 默认行为 | 本地通过 | 仅 `idle` 持续；其余 22 条播一遍回 `idle`；显式 `loop` / `pingpong` 可持续 |
| 数据与构建 | PASS | 生成检查、12 项 Python 测试、PlatformIO 构建成功 |
| 设备身份和旧版核对 | PASS | 当次 COM5、MAC `28:84:85:44:6C:00`；写前 v0.2.1 应用区读回匹配 |
| 应用区写入和读回 | PASS | 仅写 `0x10000` 应用区；v0.2.2 `verify OK (digest matched)` |
| 正常启动与自动返回 | 待验收 | 当前设备仍在下载模式；复位启动后检查 `happy` → `Return to base: IDLE` |
| 显式循环与外设 | 待验收 | 检查 `loop happy` 不自动返回及触摸、按键、IMU、振动 |

详细记录：`04-output/v0.2.2/验收记录-【codex】.md`。

## v0.2.1 历史门禁

| 门禁 | 状态 | 证据或条件 |
| --- | --- | --- |
| 用户 Grok bot 数据快照与仓库锁定提交对照 | PASS | 27 个表情和 23 条动画语义一致，用户导出 SHA-256 `6004C77DE75918EB9E5A0D71F2780B9285CD3D7B5E70CE92D1BB74305C499125` |
| 数据生成和桌面视觉总览 | PASS（桌面首帧） | 23 条动画数据表和 23 张 466px 首帧；逐帧网页/真机视觉验收尚未完成 |
| 单测与本地固件构建 | PASS | Python 11 项通过；PlatformIO 构建成功；应用镜像校验有效 |
| 设备身份复核 | PASS | COM5、`303A:1001`、MAC `28:84:85:44:6C:00` 与备份一致 |
| 应用区写入与读回校验 | PASS | 仅写 `0x10000` 应用区；修正版 `verify OK (digest matched)`；首版镜像保留 |
| 真机视觉 | 部分通过 | 用户照片确认黑体白眼；全部 23 条动画逐帧效果未验收 |
| 串口表情入口与待机性能 | PASS（限定范围） | 23/23 入口接受；待机 5 秒窗口 60.0 FPS，平均渲染约 11.07–12.04 ms |
| 外设、过渡场景性能和 30 分钟稳定性 | 待验收 | 需触摸、按键、IMU、振动、快速切换与长期运行证据 |

## v0.2.0 历史计划与未完验收

> 版本主题：KK 底座与 Grok Avatar 可编译原型  
> 日期：2026-09-16  
> 状态：首次设备写入与写后校验完成，等待真机功能验收  
> 详细 PRD：`03-v0.2.0-KK-Grok-Avatar开发PRD-【codex】.md`
> Luna Max 任务书：`04-Luna-Max目标模式开发任务书-【codex】.md`  
> 实施进度：`05-v0.2.0实施进度与验收记录-【codex】.md`

> 下一版本视觉决策：v0.2.1 使用 Bible Strong Avatar Lab 的 Grok bot 作为目标，详见 `06-v0.2.1-Grok-bot视觉对齐方案-【codex】.md`；本文件仍记录 v0.2.0 当前验收，不把新方案计作现有固件已完成。

## 1. 本版本目标

以 KK StopWatch Avatar 为硬件和动画底座，建立 PlatformIO/Arduino/M5Unified 正式工程，将首批 Grok 眼睛和 blob 几何转换为可重复生成的 C++ 数据，完成可编译固件并在明确授权后完成首次写入。

## 2. 已确认输入

- 硬件：M5Stack StopWatch C152，ESP32-S3 rev v0.2、16MB Flash、8MB PSRAM。
- KK底座提交：`204963257cb4dc2f3d7501eff900897bac55ef82`。
- Grok研究提交：`647e9bd7c60290c42a738fad586589b3f36a4680`。
- 使用范围：个人、非商业、本地使用；允许复制和转换 Grok 资产。
- 恢复能力：完整 Flash 双副本已建立并通过设备端校验。

## 3. 当前任务

| ID | 任务 | 状态 | 停止/验收条件 |
| --- | --- | --- | --- |
| DOC-02 | 编制 Luna Max 目标模式任务书 | 已完成 | 阶段、文件、命令、测试、视觉评分和停止闸门完整 |
| TRACK-01 | 建立实施进度与验收记录 | 已完成 | G0—G4、T01—T12、视觉评分和证据表可持续回填 |
| SRC-01 | 导入锁定的 KK 底座至 `03-Src/stopwatch-grok-avatar/` | 已完成 | 来源、提交和许可证可追溯 |
| DOC-01 | 建立源码 README、UPSTREAM 和第三方说明 | 已完成 | 两个上游及本地边界完整 |
| TOOL-01 | 安装 PlatformIO Core 6.1.18 | 已完成 | 项目隔离环境版本可复现 |
| BUILD-01 | 原版 KK 干净编译 | 已完成 | `L2-baseline-build.log` 成功 |
| ASSET-01 | 建立 Grok 几何转换脚本 | 已完成 | 确定性输出和负向测试通过 |
| ASSET-02 | 接入首批 blob 和8组代表性眼睛 | 已完成 | 25 组源眼睛、96 点主体和边界测试通过 |
| ANIM-01 | 将 Grok 几何映射到12种状态 | 已完成 | 12 状态映射到 8 个实现眼组并编译通过 |
| INPUT-01 | 保留触摸、IMU、按钮、振动与串口控制 | 已完成 | 静态入口检查和最终编译无回退 |
| BUILD-02 | 生成 v0.2.0 待烧录固件 | 已完成 | 固件、ELF、MAP、大小、SHA-256 和日志齐全 |
| G3 | 首次烧录人工确认 | PASS | 已收到大尾巴明确授权，并完成端口、MAC、芯片、Flash、恢复镜像预检 |
| FLASH-01 | 首次烧录 | PASS | 四段目标镜像写入成功，写后逐段 `verify_flash` 通过 |
| TEST-01 | 真机交互和性能验收 | 进行中 | 屏幕、触摸、按键、振动、IMU、串口性能和稳定性仍需人工/真机证据 |

## 4. 实施顺序

1. P0：导入底座并固化来源。
2. P1：安装工具链并复现原版编译。
3. P2：建立 Grok 资产转换链。
4. P3：完成角色集成和待烧录固件。
5. G3：停止并申请首次烧录确认。
6. P4：放行后真机烧录与验收（烧录已完成，验收进行中）。
7. P5：问题收敛、文档和交付物整理。

## 5. v0.2.0 完成定义

- 正式源码、构建说明、上游记录和许可证完整。
- 本机从干净状态成功编译原版 KK 和 Grok v0.2.0。
- Grok 几何转换可重复，源数据、坐标、三角化和负向夹具通过检查。
- 12 种状态全部完成配置并生成桌面预览和固件。
- `04-output/v0.2.0/` 已包含固件、SHA-256、构建日志、测试、视觉证据、G3 预检和 G4 写入报告。
- 首次设备写入已完成，但在核心外设和稳定性验收完成前，不将 v0.2.0 标为真机正式交付。

## 6. 当前阻塞和人工确认点

- 实物背面版本及 `BAT/5V IN` 标识尚未人工确认，但不阻塞本机编译。
- 当前使用项目隔离的 PlatformIO Core 6.1.18；系统 PATH 不承诺全局安装。
- v0.2.0 已写入角色与用户指定的网页 Grok bot 外观不一致；该差距列入 v0.2.1，不能通过把本版本视觉评分改写为“对齐”来结项。
- 首次烧录前已重新枚举端口、核对 MAC、芯片/Flash 和恢复镜像，并取得大尾巴明确确认；当前剩余人工确认是实物贴纸版本和真机功能结果。

## 7. 历史阶段

- v0.1.0：项目结构、硬件知识和安全边界完成。
- v0.1.1：M5Burner安装和烧录准备完成。
- v0.1.2：当前16MB Flash完整备份、设备端校验和第二本机副本完成。
