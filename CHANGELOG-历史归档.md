# CHANGELOG 历史归档

## v0.12.5-dev - 2026-09-24（桌面小人始终置顶）

- 控制台左侧增加“始终置顶”开关，默认保持原有置顶状态；关掉后其他窗口可覆盖悬浮小人。
- Electron 通过控制台专用 IPC 切换并将结果写入既有窗口状态文件，重启恢复；浏览器页面禁用。
- 45 项 Node 回归通过；正式 Electron 实屏与重启恢复待验收。固件、设备、语音服务未改。

## v0.12.4-dev - 2026-09-24（状态终端与双栏布局）

- 机器人下方新增只读状态终端，黑/白主题记忆、折叠、清空、时间戳、去重和120条内存上限；汇集现有状态，不新增连接或轮询，不记录聊天正文/原始异常。
- 左栏最大320px，两栏间距24px并增加分隔线，保留控制开关及桌面缩放，窄屏内部滚动。
- 46 Python、43 Node 回归通过；隔离浏览器验证主题/记忆/清空及桌面和窄屏布局，无页面错误。正式实例未重启，未操作设备或烧录；源码与运行状态分开记录。

## v0.12.2-dev - 2026-09-24（开机自动连接语音）

- 控制台加载后等待本机语音服务就绪，自动创建当前窗口的语音会话并应用已保存路由；不自动录音、播放、上传或接管已有会话。
- 服务启动较慢时后台重试；已有会话保留手动“接管会话”，隔离预览可设置 `auto_connect_voice=false`。
- 浏览器启动回归、Python 配置/健康接口回归通过；设备固件未改、未烧录。

## v0.12.1-dev - 2026-09-24（电脑双小人动画同步）

- 控制台和桌面悬浮窗口通过受限 IPC 共用表情、播放方式和启动时间；自动语音表情、角色预览及返回自动联动同步，迟到窗口可追上动画进度。
- 15 项桌面与 22 项控制台自动测试通过；正式 Electron 双窗口实屏待重启验收。设备固件、Watch 发送目标和旧发行包未改。

## v0.12.0-dev - 2026-09-23（桌面尺寸与启动蓝牙）

- 桌面小人增加 25%–150% 滑块、尺寸持久化、屏幕边界约束；小窗口只保留头像，网页头像不随滑块变化。仅受限 Electron IPC 可调整。
- 后端启动时后台扫描并自动连接配置地址或唯一匹配服务的 StopWatch；多设备/未发现/错误提示可见，手动扫描/连接/断开优先，退出清理任务。
- 版本、README/AGENTS/当前计划同步，验证及真实运行边界见 v0.12.0 验收记录；无固件修改或烧录。

## v0.11.3-dev - 2026-09-23（Gork 托盘图标）

- 用户指定 Gork 外观：黑色圆头/白色双眼/细浅色边缘；替换托盘 SVG 数据地址为原生位图、多缩放 PNG 表示和空图检测。
- 11 项桌面单元测试通过；实际 Windows 托盘显示待重启确认，未重启正式程序。语音与设备逻辑不变。

## v0.11.2-dev - 2026-09-23（输入区轻量化）

- 取消嵌套输入框、底色和阴影，采用单层细边框、较短输入区和无框次要操作；键盘提示移到框外。
- 保留控件间距与视口自适应，按用户反馈修订设计规范 21；语音与设备逻辑不变。

## v0.11.1-dev - 2026-09-23（输入区间距与设计规范）

- 模式/输入/操作/提示分层布局，桌面区块间隔 16 px，卡片留白 24 px，操作高度 40 px；响应式收紧且不贴边。
- 输入聚焦样式、禁用状态、模式分组和输入辅助说明统一；CSS 展开为可维护规则，并沉淀设计规范 21。
- 四种窗口尺寸无整体溢出、输入和按钮可见，21 项既有浏览器/音频回归通过；语音/设备行为不变。

## v0.11.0-dev - 2026-09-23（语音服务迁入当前项目）

- 根据最新明确授权，将语音 v0.3.2 源码、配置模板、测试和托管入口导入 `03-Src/voice-service/`，模型与独立环境迁入本项目忽略目录，原项目保留。
- 默认宿主启动路径从当前项目根目录计算，移除本机旧项目引用，并固定默认子进程配置到当前模块；Key 继续使用已有指定环境变量，不落盘。
- 新环境 pip check、46 项语音测试、85 项控制台/工具回归通过；真实本地 TTS → ASR、加载/复用/模型子进程退出验证通过，云请求为零。
- 自动启动正式 Electron 被策略拒绝，未绕过；用户需从根启动器进入。未改固件、设备、旧发行包。README/AGENTS/当前计划/PRD 20 与迁移记录同步。

## v0.8.4-dev - 2026-09-22（ES8311 官方初始化修复；真机已出声）

- v0.8.3 同时开启 M5IOE1 G10 与 ESP32 GPIO14 后，本地 Sound/Test 仍显示 `playing` 且无声；双 PA 门控不是充分根因。
- 对照官方 `M5StopWatch-UserDemo` 与其锁定的 Espressif `esp_codec_dev 1.5.4`，确认 M5Unified StopWatch 回调缺少 44.1 kHz 时钟分频、16-bit I2S、DAC 参考和解除静音等完整 ES8311 配置。
- v0.8.4 保留现有 I2S/DMA，完整复现官方 ES8311 寄存器序列；配置期间关闭 PA，关键寄存器读回通过后再开启 G10/GPIO14，失败显示 `codec config failed`。
- 73 项 Python、5 项浏览器、5 项桌面测试通过；PlatformIO 构建成功，RAM 63,928 bytes、Flash 1,625,873 bytes，应用镜像 1,626,240 bytes。
- 按用户当次“授权烧录 v0.8.4”重新识别 COM5、ESP32-S3 rev0.2、16 MB Flash、8 MB PSRAM、MAC `28:84:85:44:6C:00`，从设备确认 app0 `0x10000`/6400 KiB；仅写入 v0.8.4-dev 应用区，工具哈希校验及独立 `verify_flash` 均通过并硬复位。未改分区/NVS/bootloader/eFuse，未整片擦除。

## v0.8.3-dev - 2026-09-22（GPIO14 功放门控候选修复；已写入待听验）

- v0.8.2 本地 Sound/Test 显示 `playing` 仍无声；据此确认驱动启动、M5IOE1 G3/G10 锁存、ES8311 I2C 和 PCM 入队通过，但真实输出仍失败。
- 对照 M5Stack 官方 `M5StopWatch-UserDemo` 提交 `6b4aa125288b6fe9dca661f10159f6e1e5ee785c`，发现出厂实现同时控制 M5IOE1 G10 与 ESP32 GPIO14 两路扬声器功放门控；当前 M5Unified 回调缺少 GPIO14。
- 播放路径新增 GPIO14 方向/电平写入与读回诊断，codec/G10 就绪后拉高并等待 10 ms；所有停止、失败、录音切换和启动关断路径统一先拉低 GPIO14，再停止扬声器。
- 73 项 Python、5 项浏览器、5 项桌面测试通过；PlatformIO 构建成功，RAM 63,928 bytes、Flash 1,625,005 bytes，应用镜像 1,625,376 bytes。
- 按用户当次“烧录”授权重新识别 COM5、ESP32-S3 rev0.2、16 MB Flash、MAC `28:84:85:44:6C:00`，从设备确认 app0 `0x10000`/6400 KiB；仅写入 v0.8.3-dev 应用区，工具哈希校验及独立 `verify_flash` 均通过并硬复位。未改分区/NVS/bootloader/eFuse，未整片擦除；GPIO14 是高置信候选根因，仍须真实听验确认。

## v0.8.2-dev - 2026-09-22（扬声器供电分层诊断；已写入但听验失败）

- v0.8.1 本地 Sound/Test 无声后，Audio test 又发送 2 秒、64,000-byte、幅度 16000 的长 PCM；设备返回播放开始/结束，仍完全无声，排除 BLE 未传完、声音过短和单纯音量低。
- 移除应用层第二套 M5IOE1 驱动及依赖，振动统一使用 M5Unified；播放前通过同一 IO expander 恢复 codec 电源 G3 与功放 G10 的输出状态，启动后读回两路锁存和 ES8311 `0x18:0x00`。
- Sound 页增加 `Speaker` 状态，区分 driver、power/PA、codec、PCM 阶段；内置声音和长 PCM 共用同一启动函数。双驱动竞争是候选根因，真机复测前不作确认。
- 72 项 Python、5 项浏览器、5 项桌面测试通过；PlatformIO 构建成功，RAM 63,928 bytes、Flash 1,624,561 bytes，应用镜像 1,624,928 bytes。
- 按用户当次“烧录吧”授权重新识别 COM5、ESP32-S3 rev0.2、16 MB Flash、MAC `28:84:85:44:6C:00`，从设备确认 app0 `0x10000`/6400 KiB；仅写入 v0.8.2-dev 应用区，工具哈希校验及独立 `verify_flash` 均通过并硬复位。未改分区/NVS/bootloader/eFuse，未整片擦除；A5 继续 FAIL，待真实听验。

## v0.8.1-dev - 2026-09-22（短音冷启动修复；已写入但听验失败）

- 真机确认 v0.8.0 遥控播放收到 `STARTED(129)` 但无声，设备本地 Sound/Test 在 100% 音量也无声；A5 由 NOT RUN 更新为 FAIL，故障范围缩小到设备端短音启动/播放路径。
- 播放前重置共享 Mic/Speaker 状态，重新初始化 codec/扬声器并等待 30 ms；六个 PCM 增加 50 ms 静音引导，总量增至 68,864 bytes。客户端将 STARTED 视为中间事件并等待终态，不再显示裸数值 129。
- 71 项 Python、5 项浏览器、5 项桌面测试通过；PlatformIO 构建成功，RAM 64,336 bytes、Flash 1,647,885 bytes，应用镜像 1,648,256 bytes。
- 按用户当次“授权烧录”重新识别 COM5、ESP32-S3 rev0.2、16 MB Flash、MAC `28:84:85:44:6C:00`，从设备确认 app0 `0x10000`/6400 KiB；仅写入 v0.8.1-dev 应用区，写入校验及独立 `verify_flash` 均通过并硬复位。未改分区/NVS/bootloader/eFuse，未整片擦除。
- 写入与读回不能证明实际出声；需先执行设备本地 Sound/Test 听验，再测试控制台六种声音、音量、静音、停止和终态。
- 烧录后用户复测设备本地 Sound/Test 仍无声，v0.8.1 未通过 A5；冷启动稳定等待与静音引导不是充分修复。下一步先复用 Audio test 的长 PCM/PSRAM 路径做不烧录分流验证。

## v0.8.0-dev - 2026-09-21（Gork 控制台实施中；已写入待真机验收）

- 新增 Electron 44.4.3 单实例桌面壳、透明五状态角色、托盘、受限 preload、窗口位置恢复、后端身份校验与自有进程生命周期；5 项桌面单测及真实启动探针通过。
- 统一后端新增独立语音服务的外部复用/自有启动、OpenAI-compatible 文字回答适配、有界内存上下文与第三项独立上传许可；未配置时明确保持跟读模式。
- 确定性生成 6 个 CC0 原创短音效，总 PCM 59,264 bytes；固件新增 9-byte 加密声音控制、8 条终态去重、音量/静音/NVS/试听和音频互斥。最新构建 RAM 64,336 bytes、Flash 1,638,225 bytes。
- 旧 AudioClient 并入同一 BLE 连接，网页提供 PC→设备、设备→PC、设备→处理→设备三种延迟模式及真实字节/速率/阶段和取消；不把模拟结果当真机吞吐。
- 70 项 Python、5 项浏览器、5 项桌面测试通过；真实浏览器渲染与源码/Windows x64 目录包烟测通过。
- 按用户当次“烧录”授权重新识别 COM5、ESP32-S3、16 MB Flash、MAC 尾号 `6C:00` 与 app0 `0x10000`/6400 KiB；仅写入 1,638,592-byte 应用镜像，工具写入校验和独立 `verify_flash` 均通过并硬复位。未改分区/NVS/bootloader/eFuse，未整片擦除；未云调用、未推送 GitHub，真机声音/UI/BLE/稳定性仍待验收。
- 修正重复打开控制台时第二个 Electron 进程触碰首个实例缓存并在常驻命令窗打印 `0x5` 的问题：启动器改为直接拉起 GUI、不保留命令窗，未取得单实例锁的进程不再注册 IPC、窗口或后端初始化。

## v0.7.2-P0 - 2026-09-21（录音计时与故障诊断修复；已写入待实测）

- 后续用户确认一次短录音回放听到本人声音；48,000 bytes 上行1,743 B/s、下行1,636 B/s，双向传输约57秒。基础录放闭环得到用户听验证据，安全/稳定性与实时能力不因此通过。
- 后续经当次授权，重新识别 COM5 目标及实际 app0 分区，仅写 `0x10000` 应用镜像 1,575,200 bytes；写入校验、独立 verify_flash 通过并发送硬复位。未改分区/NVS/bootloader，未整片擦除。
- 修正麦克风初始化后使用旧循环时间减新入队时间导致的无符号下溢；采集重新取时，录音起点采用实际入队时刻，超时函数覆盖过期时间戳及计数回绕。
- 新增 E9–E13 区分麦克风初始化/入队/超时和扬声器初始化/入队故障；错误不会被电脑清理 CANCEL 或随后的断连覆盖，新会话/任务清除旧错误。电脑工具升至 v0.7.2，兼容旧 code 5。
- 60 项 Python、5 项 Node 测试通过；6 项 C++ 计时编译断言通过；PlatformIO 构建成功，RAM 64,064 bytes、Flash 1,574,841 bytes。不代表真机录音/回放验收。

## v0.7.1 - 2026-09-21（电脑音频测试工具：Windows 服务发现修复）

- 默认枚举实测只返回旧表情服务；无过滤 UNCACHED 查询在本机抛 0x8000FFFF，指定音频 service 的 UNCACHED 查询成功返回服务及两个特征。仅客户端改为 services=[audio UUID] + winrt.use_cached_services=False，不清配对、不重刷固件。
- 缺少服务的提示不再断言旧固件；握手尚未成功时退出不发送 CANCEL。新增连接选项与错误退出测试，音频工具 18 项模拟测试通过。
- 设备仍 v0.7.0-P0、网页仍 v0.6.0；用户确认能进入 Audio test。服务发现不等于完整握手、收音/出声或吞吐验收。
- 用户重开蓝牙后的补测：两次连接 HELLO/CAPS、PING 回执通过（244 bytes），完成一次重连并释放连接；没有启动录音或播放，音频听验与吞吐仍待测。

## v0.7.0-P0 - 2026-09-21（纯 BLE 设备音频探针；已写入，运行待验收）

- 在现有固件新增 Settings → Audio test，电脑 ARM 后本地按住 A 录音，松开结束、B 停止；16 kHz PCM16 单声道、最长 10 秒，录放互斥。使用锁定的官方 M5Unified StopWatch 驱动，不改底层引脚、电源或分区配置。
- 新增独立加密音频 GATT、定向通知、连接/会话/事务隔离、4 包固定队列、480 KB PSRAM 上限、取消优先和 15 秒租约；信用窗口 1，协商 20–244 bytes，尚不宣称实时吞吐。
- 新增 Windows `start-ble-audio-test.cmd` 与测试工具：测试音、设备→电脑→设备 echo、完整短 WAV 播放、显式保存录音。无 Wi-Fi、云端或语音服务请求，不改变 8766 电脑音频模式。
- 固件编译通过，新增 16 项客户端/协议模拟测试，保留原 45 项回归；真实录放、BLE 吞吐/安全及 P3 服务集成待后续门禁。工作台仍 v0.6.0。
- 当次“烧录测试版”授权后，重新识别设备与既有 app0 分区，只写 `0x10000` 应用镜像 1,574,960 bytes；工具写入校验、独立 `verify_flash` 均通过并硬复位。未改分区/NVS/bootloader 或整片擦除；5 秒 BLE 扫描未发现广播，正常启动与音频运行仍需人工确认。
- 更新 README、AGENTS、PRD/当前计划、协议/操作文档与实施记录；不计算例行文件哈希、不自动烧录、不推送 GitHub。

## v0.6.0 - 2026-09-20（电脑语音工作台；不更换固件）

- 完成当前 16 MB Flash 开发前备份，两次独立读取逐字节一致，应用区对应 v0.5.0；备份仅本地保存，不计算文件 SHA-256。本轮无设备写入。
- 按交接文档 v1.0.0 在独立目录实现 Python 同源后端 + 网页工作台：电脑录音、转写编辑、完整 WAV 回读、独立上传许可、会话占用/接管、立即停播、取消与旧 generation 隔离；语音服务通过 HTTP 使用，不复制模型/venv/Key。
- 抽取共用 BLE 模块，保留原控制台入口；严格匹配回执、超时断开隔离，文字事务串行，工作台最多两次重连且不重放旧气泡。提供根目录双击启动器、配置示例和完整依赖锁定。
- 45 项自动测试通过，含 20 轮旧请求丢弃、20 次活动播放器停止；真实本地 TTS→ASR、浏览器回读、第二会话占用和未授权云路径拒绝通过。本轮扫描未发现 BLE 设备；真人录音/听感、实屏与云请求仍待验收。
- 更新 README、AGENTS、PRD/当前计划和验收记录；固件源码/设备仍 v0.5.0，不重新构建或烧录，旧固件未完成门禁继续保留。

## v0.4.0 - 2026-09-17（happy-work 派生表情；已构建，未烧录）

- 在 23 条 Grok bot 来源动画之外新增第 24 个可调用表情 `happy-work`：原样复用 `happy` 的 4 帧眼睛时间轴，下半部程序化绘制纸张、笔、握笔小手和逐渐延伸的笔迹；沿用局部擦除，退出时清除书写层。
- 默认播完回待机；`loop happy-work` 与 `pingpong happy-work` 为显式持续模式。USB 串口与 BLE 控制台均可输入带连字符的命令，最长示例为 19 字节，未改变 BLE 20 字节上限。
- 客户端、来源目录及几何测试共 17 项通过；PlatformIO 正式构建成功，RAM 62,672/327,680（19.1%），Flash 1,541,201/6,553,600（23.5%）。版本化应用镜像已归档，设备仍运行 v0.3.0；书写观感、残影和帧率待真机验收。
- 新增 v0.4.0 派生表情 PRD，更新 README、AGENTS、当前版本计划及使用说明。v0.3.0 BLE 安全/稳定性缺项未因此关闭。

## v0.3.0 - 2026-09-17（BLE 配对与核心表情控制通过）

- 在现有 StopWatch 固件内新增 BLE GATT 控制模块和 Bluetooth 设置页：BLE 默认关闭，支持本地 120 秒 Pair 窗口、secure-connections bonding、加密特征权限、单控制端绑定和二次确认清除绑定。
- BLE 回调只做固定长度校验/入队；USB 与 BLE 共用 `AvatarEngine::showFromCommand`，Settings/Hardware/Bluetooth 菜单打开时返回 `ERR:BUSY`，未知输入不改表情、不振动。
- 新增 Windows Bleak 控制台、双击启动脚本、锁定依赖和 mock GATT 测试；5 项客户端测试通过。L1 探针与正式 PlatformIO 构建通过，正式镜像 RAM 19.1%、Flash 23.5%。
- 已完成 G3 预检，并在获得“执行烧录”授权后仅写入 `0x10000` 应用区；写后 `verify_flash` 返回 `verify OK (digest matched)`。未整片擦除、未改 bootloader、分区表、eFuse 或安全配置。
- 设备本地打开 `BLE ON` 并点击 `Pair` 后，Windows `BleakClient(pair=True)` 已发现并连接 `GorkBot-SW`，状态通知、菜单 `ERR:BUSY`、23/23 标准命令、播放前缀和一次断线重连均通过；陌生端拒绝、10 次重连、P95/FPS、功耗和 30 分钟稳定性仍待补测。

## v0.2.7 - 2026-09-17（v0.3.0 BLE PRD 与 Luna Max 交接，历史）

- 新增 `07-v0.3.0-BLE表情操控开发PRD-【codex】.md`：明确本电脑内置蓝牙 + StopWatch BLE GATT、23 表情、Windows Bleak 控制台、配对安全、USB 回退及 L0—L6/G0—G5 门禁。
- 本机确认 Windows 11 build 26200、Intel Wireless Bluetooth 与 Microsoft BLE 枚举器状态 OK、Python 3.14.3 和 Bleak 3.0.2；尚未建立 StopWatch BLE 广播、配对或无线控制。
- 更新 README、项目 AGENTS 与当前版本计划。文档版本 v0.2.7，设备固件仍 v0.2.5；本轮未改固件、未烧录或改变电脑蓝牙配对。

## v0.2.6 - 2026-09-17（Windows 表情串口控制台）

- 新增可双击的 `tools/start-expression-console.cmd` 与 PowerShell 串口控制脚本；自动识别 StopWatch USB 串口，不写死 COM5，使用 115200 波特率。
- 支持 23 个固件表情名、`once/loop/pingpong` 播放前缀、`:list` 和 `:q`；关闭 Settings 后使用，不与其他串口程序同时运行。
- PowerShell 语法、端口识别及真机发送测试通过：`happy` 获得 `Command accepted: HAPPY`。工具窗口已启动；本版未修改或重烧设备固件，设备仍为 v0.2.5。

## v0.2.5 - 2026-09-17（调试小字移至顶部，已写入待真机验收）

- 调试模式动画名由头像底部移至黑色圆体顶部，中心 y=42；字体、颜色、开关与表情切换逻辑不变。
- 数据生成检查、12 项现有 Python 测试和 PlatformIO 构建通过；固件 907184 字节。
- 当次重新识别 COM5、MAC `28:84:85:44:6C:00`，仅写 `0x10000` 应用区，读回 `verify OK` 并发出硬复位；顶部文字观感与其他真机功能待大尾巴验收。
- 按大尾巴要求，后续常规迭代不再重复计算或核验文件 SHA-256；仍保留必要的设备识别、写入校验和读回验证。

## v0.2.4 - 2026-09-17（设置页电量与调试模式，已写入待真机验收）

- 设置首页新增电池电量估算百分比和电压，调用 M5Unified 的 StopWatch/M5PM1 接口，每 5 秒刷新；无效读数显示 `--`。该百分比来自电压估算，并非精确 mAh 容量。
- 设置首页新增可持久化的 `Debug mode` 开关；开启后头像底部以小白字显示当前动画英文名，关闭后重新绘制清除。原硬件检查保留在 `Hardware Check` 子页。
- 整合尚未写入的 v0.2.3 亮度调节；v0.2.4 数据生成检查、12 项 Python 测试和 PlatformIO 构建通过。固件 907200 字节，SHA-256 `CBCE21D83F01890AB649000EA5FE41806A146357C61F46EC122F48B53830EA4A`。
- 当次核对 COM5、MAC `28:84:85:44:6C:00`、ESP32-S3/16MB/8MB、完整备份哈希，写前 v0.2.2 应用区 `verify OK`；仅写 `0x10000` 应用区，写后 v0.2.4 `verify OK`。电量实际读数、调试字样观感、开关持久化、亮度及自动回待机仍待大尾巴真机验收。

## v0.2.3 - 2026-09-17（设置页亮度调节，独立固件未写入，功能并入 v0.2.4）

- 将原 A+B 长按硬件检查页改为 `Settings`，保留 Display、IMU、Touch、Vibration、Last event 和 A/B 诊断操作；新增触屏 `−/+` 亮度调节。
- 亮度默认 150、步长 15、范围 30–255；调整立即生效并存入 ESP32 NVS，启动时读取。最低亮度保护避免调成全黑。
- 数据生成检查、12 项 Python 测试和 PlatformIO 构建通过；固件 893936 字节，SHA-256 `8611E5A3392DC0323161E34772332B1CFEBA62A2B9D47B0AD4F7B38DFE1DE8E0`。
- 此版独立固件未写入设备；亮度功能已随 v0.2.4 写入，真机触控、亮度变化、重启持久化、原诊断和自动回待机仍待实测。

## v0.2.2 - 2026-09-17（反应自动返回待机，运行时待验收）

- 修复 v0.2.1 将所有 23 条动画都设为持续状态的问题：仅 `idle` 默认循环，其他 22 条默认播放一遍后回 `idle`；串口显式 `loop` / `pingpong` 保持持续播放，`once` 播完回 `idle`。
- 新增回归测试，先复现旧行为失败，再修改生成器与串口播放入口；生成检查和 12 项 Python 测试通过，PlatformIO 构建成功。
- v0.2.2 应用固件 890352 字节，SHA-256 `5B715113EACE45F0201534EA7B139F5B245017236BAC17AF47D597342AA72C9A`。
- 当次核对 COM5、MAC `28:84:85:44:6C:00`、ESP32-S3/16MB/8MB 和完整备份哈希；写前 v0.2.1 应用区 `verify OK`，仅写 `0x10000` 应用区，写后 v0.2.2 `verify OK`。未执行整片擦除或修改 bootloader、分区表及安全配置。
- 首次运行时串口烟测无应用日志，随后确认设备仍停在下载模式；正常启动后的自动返回和外设验收尚未完成。

## v0.2.1 - 2026-09-16（应用区写入与读回通过，待真机验收）

- 确认 Bible Strong Avatar Lab 的 Grok bot 为下一版视觉与动画基准，锁定研究提交 `79fe9ba06e4874b11394b8e8a3f2c493c9d197ba`。
- 新增 v0.2.1 视觉对齐方案，明确当前白主体/黑眼与目标黑主体/白眼、27 preset / 23 sequence 的差距、实施顺序和验收门禁。
- v0.2.0 真机功能验收继续单列；不能把 v0.2.1 的编译结果算作真机验收。
- 接收大尾巴从网站导出的 Grok bot Studio 项目，并验证其 Grok bot 几何、27 个表情参数及 23 条动画与锁定仓库默认数据语义一致；以用户导出作为正式源快照。
- 新增确定性数据转换器、23 条序列固件表、黑体白眼渲染、23 张首帧预览和数据测试；保留原硬件输入、电源控制及局部刷新管线。
- Python 测试 11 项通过；短路径 PlatformIO 构建成功。首版 890016 字节（SHA-256 `217155840E13FED1AE6CD3D2666C59A22720CFA6D3675F92D6B97EB1D074BA51`）已保留为 `-initial`；修正版 890352 字节（SHA-256 `A6832890B5613A8523456A2AC73972DE3A55E3A8ED249FE690B391A9C7F761D1`）为当前设备镜像。
- 设备进入下载模式后重新枚举为 COM5、`VID_303A:PID_1001`、MAC `28:84:85:44:6C:00`；写前确认旧 v0.2.0 应用区与归档镜像一致。
- 仅写入 `0x10000` 应用区；写入工具报告哈希通过，写后 `verify_flash` 返回 `verify OK (digest matched)`。实际擦除范围 `0x00010000–0x000e9fff`；未执行整片擦除，未改 bootloader/分区表及原始完整 Flash 备份。
- 真机视觉、输入和性能尚未验收。写后串口 16 秒观察窗口无输出，不能据此认定应用成功或失败；等待实物画面及后续诊断。
- 首版串口实测仅约 45–46 FPS；将左右眼脏矩形拆开后重新编译、写入并读回校验。修正版待机 5 秒窗口为 60.0 FPS、平均渲染约 11.07–12.04 ms；23/23 源序列串口入口通过。
- 大尾巴提供真机照片并报告黑体白眼已显示；单张抓拍尚不能证明所有动画与网页逐帧一致，外设和 30 分钟稳定性仍待验收。

## v0.2.0 - 2026-09-16（首次写入完成，等待真机功能验收）

### 新增

- 选定 `KK StopWatch Avatar` 作为 Grok 风格圆屏表情机器人的工程底座，并锁定上游提交。
- 新增 v0.2.0 详细开发 PRD，明确产品范围、技术架构、目录规划、任务拆分、性能指标、风险和验收门禁。
- 更新产品总览与当前版本计划，确定 PlatformIO/Arduino + M5Unified 技术路线。
- 记录个人、非商业、本地使用 Grok 参考资产的项目边界：允许直接复制和转换，不作为本版本开发阻塞项；工程按未来可开源维护，仍保留来源和资产隔离。
- 新增 Luna Max 目标模式开发任务书，将实施拆分为 L0—L7、G0—G3，并细化几何转换、文件责任区、测试、视觉评分、证据留存和停止条件。
- 新增 v0.2.0 实施进度与验收记录模板，供目标模式持续回填实际命令、退出码、哈希、截图、阻塞和 G3 报告。
- 导入 KK 锁定基线 `204963257cb4dc2f3d7501eff900897bac55ef82`，建立 `kk-grok-v0.2.0` 本地开发分支和来源记录。
- 新增 Grok 几何源数据层、确定性 Python 转换器、固定点 C++ 几何、8 组实现眼睛映射和 `GrokRenderer` 固件渲染器。
- 完成 12 状态 SVG/HTML 桌面预览、3 组关键帧对照、浏览器实视证据和每状态视觉评分。
- 完成项目隔离 PlatformIO Core `6.1.18`、原版 KK 基线构建、最终清理后构建、测试、不变量审计和 v0.2.0 固件产物。
- 在收到大尾巴明确授权后，重新识别 `COM5`、ESP32-S3 rev `v0.2`、MAC `28:84:85:44:6C:00` 和 16MB Flash；完成首次设备写入。
- 写入 bootloader、既定 `default_16MB.csv` 分区表、`boot_app0` 和 v0.2.0 应用；四段写后 `verify_flash` 均返回 `verify OK (digest matched)`。

### 验证与边界

- 已核对 KK 上游工程配置、依赖提交、功能说明和 GitHub Actions 成功记录。
- 已核对 Grok Icon Study 的几何数据规模与锁定提交。
- 原版和 Grok 固件均已在本机完成构建验证；最终 Flash map 使用量为 `906229` bytes，RAM 为 `29888` bytes。
- 已生成固件、ELF、MAP、bootloader、分区镜像、SHA-256、测试日志、视觉证据、G3 预检和 G4 写入报告；源码工作树尚未提交。
- 未执行整片擦除、eFuse、安全启动或 Flash 加密；写入过程中仅擦除了四个目标镜像覆盖的 Flash 扇区，这是 `write_flash` 的必要操作。
- 真机屏幕、触摸、按键、振动、IMU、串口性能和 30 分钟稳定性尚未完成；串口应用启动日志受 USB CDC DTR/RTS 控制线影响，未作为通过证据。
- 根项目当前未初始化 Git；正式源码仅保留上游锁定提交和未提交工作树状态，本次未创建本地归档提交。

## v0.1.2 - 2026-09-16

### 新增

- 新增当前 StopWatch 的 16MB 完整 SPI Flash 原始镜像。
- 新增设备信息、SHA-256 校验文件、恢复说明和带人工口令保护的恢复脚本。
- 在 `C:\Users\zgzzm\Documents\M5Stack-Backups\` 建立第二份本机副本。

### 验证

- 只读识别设备为 ESP32-S3 rev v0.2、16MB Flash、8MB PSRAM。
- 完整读取 `0x000000–0xFFFFFF`，镜像大小为 `16777216` 字节。
- 镜像 SHA-256：`ACC86A969D2C201564A0910654261381A7A7D9F3D579A692908B24799626B87B`。
- esptool `verify_flash` 返回 `verify OK (digest matched)`。
- 项目副本与 Documents 第二副本的 SHA-256 完全一致。
- 未擦除或写入 Flash，未读取或修改 eFuse。

## v0.1.1 - 2026-09-16

### 新增

- 从 M5Stack 官方 CDN 获取并安装 M5Burner v3.0 到当前用户程序目录。
- 新增开始菜单快捷方式和 M5Burner 烧录准备笔记。
- 补充 UIFlow2 Web、StopWatch UIFlow2 和 M5Stack 下载页索引。

### 验证

- 校验下载包 SHA-256：`818A4983C87BF58F1ABAB077DB3F5BB3EC0C279468648300CEF9362FA9D2C69B`。
- 验证 ZIP 无绝对路径或 `..` 路径穿越项；Windows Defender 自定义扫描未发现相关威胁。
- 验证 M5Burner 正常启动，文件版本为 `3.0.0`，在线界面版本为 `v202605221800`，并可见 `STOPWATCH` 分类。
- 复核目标设备仍枚举为 `COM5`、`VID_303A&PID_1001`、状态 `Started`。
- 本版本未下载目标固件、未擦除 Flash、未执行烧录。

## v0.1.0 - 2026-09-16

### 新增

- 初始化轻量嵌入式软件开发项目结构。
- 新增 README、项目 AGENTS、产品总览 PRD 和当前版本计划。
- 新增 StopWatch 硬件与开发知识基线及官方资料索引。
- 新增源码、输出、素材、临时文件和工具目录说明。

### 验证

- 确认项目初始化前目录为空，不存在需保留的用户文件。
- 复核 Windows 当前可见的 ESP32-S3 USB JTAG/串口设备及 `COM5` 状态。
- 本版本仅创建项目文件，未向硬件写入任何内容。

## v0.5.0 - 2026-09-17（BLE 文字聊天气泡；已写入并读回校验）

- Windows 蓝牙命令行新增 `:say 文字` 与 `:clear`；中文/英文以 UTF-8 分包发送，最多 24 字符、72 字节，不改变原有 20 字节 GATT 单包上限或服务 UUID。每包等待对应回执，未收齐或校验失败不更新显示。
- 固件主循环重组并校验文字；表情画面上方以 16 px 中文字体绘制两行气泡，10 秒后自动清除，保留原表情和顶部调试标签。BLE 回调仍只做授权检查和有界入队。
- 21 项 Python 测试、来源目录校验及 PlatformIO 正式构建通过；RAM 62,912/327,680（19.2%），Flash 1,544,681/6,553,600（23.6%）。归档 `04-output/v0.5.0/firmware/firmware-v0.5.0-ble-chat-bubble.bin`（1,545,040 字节）。按大尾巴当次指令，仅写设备应用区 `0x10000`，写入校验及独立 `verify_flash` 均通过；中文实屏、气泡残影、帧率和无线真机链路待验收。
- 新增 v0.5.0 PRD/验收记录，更新 README、AGENTS 和当前版本计划。v0.3.0 的安全负向与长期稳定性缺项、原表情命令旧回执误判和自动重连问题仍未关闭。

## v0.8.5-dev - 2026-09-23（内置声音数字幅度提高 3 倍；已写入待听验）

- 用户确认 v0.8.4 本地 Sound/Test 已真实出声，但 100% 音量偏小；六个内置 PCM16 声音实测峰值均为 10,000，存在 3.2767 倍无数字削波余量。
- 生成器峰值提高到 30,000，即数字幅度精确 3 倍、约 +9.54 dB；保留 50 ms 静音引导、淡入淡出、频率、时长、声音 ID 和 0–100 音量协议。
- ES8311 继续保持 0 dB，不提高全局 codec 增益，因此动态语音、BLE PCM 和录音回放不受本次修改影响。
- 六个 WAV 峰值和 PCM16 范围检查通过；73 项 Python、5 项浏览器、5 项桌面测试通过。PlatformIO 构建成功，RAM 63,928 bytes、Flash 1,625,089 bytes，应用镜像 1,625,456 bytes。
- 按用户当次“授权烧录 v0.8.5”重新识别 COM5、ESP32-S3 rev0.2、16 MB Flash、8 MB PSRAM、MAC `28:84:85:44:6C:00`，从设备确认 app0 `0x10000`/6400 KiB；仅写入 v0.8.5-dev 应用区，工具哈希校验及独立 `verify_flash` 均通过并硬复位。未改分区/NVS/bootloader/eFuse，未整片擦除。真机必须从 30%–40% 音量逐级试听，检查实际响度、破音、爆音、底噪和异常发热。

## v0.8.6-dev - 2026-09-23（内置短音 codec +9.5 dB；响度真机通过）

- 用户确认 v0.8.5 在 100% 音量仍不够大；当前 PCM 峰值 30,000 已接近 16-bit 上限，不能再直接乘 3。
- 仅六个内置短音将 ES8311 REG32 从 `0xBF`（0 dB）提高到 `0xD2`（+9.5 dB，名义电压约 2.99 倍）；动态语音、BLE PCM 和录音回放保持 0 dB。Settings 首页新增 `v0.8.6-dev` 版本显示。
- 75 项 Python、5 项浏览器、5 项桌面测试通过。PlatformIO 构建成功，RAM 63,928 bytes、Flash 1,625,497 bytes，应用镜像 1,625,856 bytes。
- 按用户当次“做完烧录即可”授权，重新识别 COM5、ESP32-S3 rev0.2、16 MB Flash、8 MB PSRAM、MAC `28:84:85:44:6C:00`，从设备实读确认 app0 `0x10000`/6400 KiB；仅写入 v0.8.6-dev 应用区，工具哈希校验及独立 `verify_flash` 均通过并硬复位，COM5 重新枚举正常。未改分区/NVS/bootloader/eFuse，未整片擦除。
- 名义 codec 增益不等于听感严格 3 倍；当前波形接近满幅，须从 20%–30% 逐级试听，高音量可能削波或饱和。
- 写入后用户真机确认声音达到正常大小，最大响度不足问题关闭；本次确认不扩展为六种声音逐项音质、版本文字实屏、发热或长期稳定性通过。


## v0.8.7-dev - 2026-09-23（电脑端默认授权与桌面/网页角色统一）

- 按用户明确要求，三项上传许可默认勾选，连接时自动应用；记住独立撤销，不自动切云端/录音，不修改外部语音服务。
- 移除桌面自绘CSS横眼/绿边/米色底板，网页和桌面共用正式Watch来源27姿态/23动画及Canvas渲染器。增加确定性导出和副本一致性检查。
- 75 Python + 7 浏览器 + 7 桌面测试通过；浏览器检查默认checked、网页与240×280桌面外观预览。原生Electron重启后及与实物并排观感待用户确认。
- 设备保持v0.8.6-dev，未改固件或烧录；源码启动器重开生效，旧发行包未更新。README/AGENTS/PRD/计划/验收记录同步，日志仍保留5版。


## v0.8.8-dev - 2026-09-23（音色、试听与自动托管语音服务）

- 文档追加：编写下一轮 v0.9.0 四页界面重组 PRD 16、Goal 任务书 17 与语音模块复用补充 18（文档 v1.0.0），明确功能映射、M0–M5、28 项 UI/控制验收与 8 项语音复用验收；同步 README/AGENTS/当前计划。语音继续使用独立 HTTP 服务，下一轮提取客户端音频模块和独立调用示例。仅方案交付，尚未实施或提升程序版本，不新增发布条目。
- 控制台按能力目录选择本地/云端音色，支持本地语速、应用保存和固定文字试听；试听不走回答、不覆盖编辑框，沿用会话、上传许可、停止与迟到结果丢弃。
- 独立语音服务 v0.3.2 新增 cloud_voice，开放 6 个官方音色；旧服务兼容、云端自然语速限制。模型/venv/Key 不迁移，未进行云端真实调用。
- 本机启动入口已配置；后台启动、状态提示、重试、仅关闭自有进程及模型子进程。修复 Windows 阻塞 stdin 导致模型启动停滞；真实隔离端口启动/加载/退出通过。
- 80 项 Python、10 项浏览器逻辑、7 项桌面、45 项独立服务测试通过。运行/听验边界见 v0.8.8 报告；旧发行包未更新，设备仍 v0.8.6-dev，无烧录。


## v0.9.0-dev - 2026-09-23（四页 Gork 控制台与语音模块边界）

- v1.0.3 原型追加：机器人下方增加状态与三个快捷开关；按用户要求默认云端朗读、本地识别，云端语速为自然 1.0×。仅原型及规划更新，未更改正式服务偏好或发起云请求。

- 原型 v1.0.3（正式程序未改）：按用户最新反馈，取消 v1.0.2 上方头像方案，采用左侧常驻机器人、右侧四模块标签页；切换不隐藏或重建机器人，保留底部固定操作。同步规划，结构与脚本检查通过，视觉待确认。

- 原型继续迭代 v1.0.2（正式程序未改）：按用户两处标注，将机器人移到工作区上方右侧，模块入口改为内容区标题位置的四个标签，仅切换下方内容；取消左侧大头像和侧面板。同步 PRD 19/当前计划，结构与脚本检查通过，视觉待验。

- 设计追加（未实施）：按用户要求编写下一轮单页自适应工作台 PRD 19，删除大标题介绍、采用固定输入和角色/设备/设置侧面板；交付离线交互原型 v1.0.0 与布局验证记录；按用户后续反馈迭代为 v1.0.1，恢复常驻动画机器人、聊天缩到右侧约六成，窄屏保留小型角色。本次 v1.0.1 只完成文件与脚本检查，浏览器预览被安全策略拦截。建议下一目标 v0.10.0-dev，当前程序版本不变，不把原型模拟当真实服务/设备验收。

- 控制台重组为对话、角色、StopWatch、设置四页；全局状态和停止入口保持可见，实验音频与诊断收进设备页。
- 工作台控制权与语音会话分离：语音离线或被释放后仍可本地预览和操作已连接设备；工作台保留 30 秒租期与显式接管。
- 新增手动角色/气泡接口与桌面接收。`happy-work` 明确只支持 Watch；桌面一次播放回 idle，气泡 8 秒自动清除。
- 提取浏览器 `Recorder` / `WavPlayer`，交付独立 HTTP 示例与接入说明；引擎、模型、venv、Key 继续保留在独立语音仓库。
- 83 Python、11 浏览器 Node、7 Electron 单元测试通过；隔离 8876 页面四页检查和 390 宽无横向溢出通过。临时 8875 独立语音服务完成真实本地 TTS/ASR 与单活会话 409/释放回归。未烧录、未发云端请求、未重启正式实例，真实 Electron/麦克风/设备门禁见 v0.9.0 验收记录。


## v0.10.0-dev - 2026-09-23（左侧机器人与自适应标签工作台）

- 按用户确认的 v1.0.3，由 Astra 完成正式界面，Sol 接手回归：左侧常驻动画、真实状态与三个开关；右侧四模块标签；输入贴合窗口下沿，内容内部滚动。
- 默认云端 TTS/本地 ASR，保留旧偏好与撤销许可；不自动录音/上传。消息与输入分离，支持回答复制和独立朗读。
- 修复重复录音触发、切页/失焦录音取消、声音试听迟到结果；设置草稿与已应用路径区分，撤销立即取消。设备控制不依赖语音在线。
- Electron 增加仅控制台可调用的窗口可见性 IPC；源码与元数据升级，旧运行实例/发行包、固件及外部语音服务未改。
- 验证与人工边界见 `04-output/v0.10.0/`。README、AGENTS、当前计划及 PRD 19 同步。

## v0.12.3-dev / 固件 v0.8.7-dev - 2026-09-24（BLE 程序内配对与重连）

- 对照旧 ESP32-S3 项目确认 StopWatch 需要安全绑定，但电脑端 `pair=True` 可在 Gork 内发起首次配对，无须先到 Windows 设置添加设备。
- 实机扫描可见目标广播，Windows GATT 发现两种路径均报 `Unreachable`。固件将绑定地址比较移到加密认证完成后，以允许已绑定电脑使用临时私有地址重连；未认证写入仍被拒绝。
- 电脑端延长首次配对等待并保留真实服务发现错误；新增首次失败及掉线后的持续退避重连，手动断开终止重试。BLE 相关 40 项、控制台完整目录 46 项回归及固件构建通过。
- 经当次授权仅写入 Watch 的 app0 应用区，独立 verify_flash 通过。旧绑定的 GATT 仍不可达，Windows 缓存服务返回访问被拒绝；手表清绑定、电脑端 Bleak 取消旧配对并在程序内重新配对后，GATT 正常发现。已连续完成 10 次断开重连；正式 Gork v0.12.3 重启后自动连接成功。菜单打开时命令返回预期的 `ERR:BUSY`，菜单关闭后的回执及长稳待验收。
- 固化程序内修复旧配对工具，显式 `--apply` 才修改 Windows 配对记录；仅对唯一且匹配的 Watch 生效，不改正常重连路径。
