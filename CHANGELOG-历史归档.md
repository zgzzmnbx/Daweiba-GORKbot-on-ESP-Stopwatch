# CHANGELOG 历史归档

## v0.2.6 - 2026-09-17（Windows 表情串口控制台）

- 新增可双击的 `tools/start-expression-console.cmd` 与 PowerShell 串口控制脚本；自动识别 StopWatch USB 串口，不写死 COM5，使用 115200 波特率。
- 支持 23 个固件表情名、`once/loop/pingpong` 播放前缀、`:list` 和 `:q`；关闭 Settings 后使用，不与其他串口程序同时运行。
- PowerShell 语法、端口识别及真机发送测试通过：`happy` 获得 `Command accepted: HAPPY`。工具窗口已启动；本版未修改或重烧设备固件，设备仍为 v0.2.5。

## v0.2.5 - 2026-09-17（调试小字移至顶部，已写入待真机验收）

- 调试模式动画名由头像底部移至黑色圆体顶部，中心 y=42；字体、颜色、开关与表情切换逻辑不变。
- 数据生成检查、12 项现有 Python 测试和 PlatformIO 构建通过；固件 907184 字节。
- 当次重新识别 COM5、MAC `28:84:85:44:6C:00`，仅写 `0x10000` 应用区，读回 `verify OK` 并发出硬复位；顶部文字观感与其他真机功能待大尾巴验收。
- 按大尾巴要求，后续常规迭代不再重复计算或核验文件 SHA-256；仍保留必要的设备识别、写入校验和读回验证。

## v0.2.4 - 2026-09-17（设置页电量与调试模式，已写入待真机验收）

- 设置首页新增电池电量估算百分比和电压，调用 M5Unified 的 StopWatch/M5PM1 接口，每 5 秒刷新；无效读数显示 `--`。该百分比来自电压估算，并非精确 mAh 容量。
- 设置首页新增可持久化的 `Debug mode` 开关；开启后头像底部以小白字显示当前动画英文名，关闭后重新绘制清除。原硬件检查保留在 `Hardware Check` 子页。
- 整合尚未写入的 v0.2.3 亮度调节；v0.2.4 数据生成检查、12 项 Python 测试和 PlatformIO 构建通过。固件 907200 字节，SHA-256 `CBCE21D83F01890AB649000EA5FE41806A146357C61F46EC122F48B53830EA4A`。
- 当次核对 COM5、MAC `28:84:85:44:6C:00`、ESP32-S3/16MB/8MB、完整备份哈希，写前 v0.2.2 应用区 `verify OK`；仅写 `0x10000` 应用区，写后 v0.2.4 `verify OK`。电量实际读数、调试字样观感、开关持久化、亮度及自动回待机仍待大尾巴真机验收。

## v0.2.3 - 2026-09-17（设置页亮度调节，独立固件未写入，功能并入 v0.2.4）

- 将原 A+B 长按硬件检查页改为 `Settings`，保留 Display、IMU、Touch、Vibration、Last event 和 A/B 诊断操作；新增触屏 `−/+` 亮度调节。
- 亮度默认 150、步长 15、范围 30–255；调整立即生效并存入 ESP32 NVS，启动时读取。最低亮度保护避免调成全黑。
- 数据生成检查、12 项 Python 测试和 PlatformIO 构建通过；固件 893936 字节，SHA-256 `8611E5A3392DC0323161E34772332B1CFEBA62A2B9D47B0AD4F7B38DFE1DE8E0`。
- 此版独立固件未写入设备；亮度功能已随 v0.2.4 写入，真机触控、亮度变化、重启持久化、原诊断和自动回待机仍待实测。

## v0.2.2 - 2026-09-17（反应自动返回待机，运行时待验收）

- 修复 v0.2.1 将所有 23 条动画都设为持续状态的问题：仅 `idle` 默认循环，其他 22 条默认播放一遍后回 `idle`；串口显式 `loop` / `pingpong` 保持持续播放，`once` 播完回 `idle`。
- 新增回归测试，先复现旧行为失败，再修改生成器与串口播放入口；生成检查和 12 项 Python 测试通过，PlatformIO 构建成功。
- v0.2.2 应用固件 890352 字节，SHA-256 `5B715113EACE45F0201534EA7B139F5B245017236BAC17AF47D597342AA72C9A`。
- 当次核对 COM5、MAC `28:84:85:44:6C:00`、ESP32-S3/16MB/8MB 和完整备份哈希；写前 v0.2.1 应用区 `verify OK`，仅写 `0x10000` 应用区，写后 v0.2.2 `verify OK`。未执行整片擦除或修改 bootloader、分区表及安全配置。
- 首次运行时串口烟测无应用日志，随后确认设备仍停在下载模式；正常启动后的自动返回和外设验收尚未完成。

## v0.2.1 - 2026-09-16（应用区写入与读回通过，待真机验收）

- 确认 Bible Strong Avatar Lab 的 Grok bot 为下一版视觉与动画基准，锁定研究提交 `79fe9ba06e4874b11394b8e8a3f2c493c9d197ba`。
- 新增 v0.2.1 视觉对齐方案，明确当前白主体/黑眼与目标黑主体/白眼、27 preset / 23 sequence 的差距、实施顺序和验收门禁。
- v0.2.0 真机功能验收继续单列；不能把 v0.2.1 的编译结果算作真机验收。
- 接收大尾巴从网站导出的 Grok bot Studio 项目，并验证其 Grok bot 几何、27 个表情参数及 23 条动画与锁定仓库默认数据语义一致；以用户导出作为正式源快照。
- 新增确定性数据转换器、23 条序列固件表、黑体白眼渲染、23 张首帧预览和数据测试；保留原硬件输入、电源控制及局部刷新管线。
- Python 测试 11 项通过；短路径 PlatformIO 构建成功。首版 890016 字节（SHA-256 `217155840E13FED1AE6CD3D2666C59A22720CFA6D3675F92D6B97EB1D074BA51`）已保留为 `-initial`；修正版 890352 字节（SHA-256 `A6832890B5613A8523456A2AC73972DE3A55E3A8ED249FE690B391A9C7F761D1`）为当前设备镜像。
- 设备进入下载模式后重新枚举为 COM5、`VID_303A:PID_1001`、MAC `28:84:85:44:6C:00`；写前确认旧 v0.2.0 应用区与归档镜像一致。
- 仅写入 `0x10000` 应用区；写入工具报告哈希通过，写后 `verify_flash` 返回 `verify OK (digest matched)`。实际擦除范围 `0x00010000–0x000e9fff`；未执行整片擦除，未改 bootloader/分区表及原始完整 Flash 备份。
- 真机视觉、输入和性能尚未验收。写后串口 16 秒观察窗口无输出，不能据此认定应用成功或失败；等待实物画面及后续诊断。
- 首版串口实测仅约 45–46 FPS；将左右眼脏矩形拆开后重新编译、写入并读回校验。修正版待机 5 秒窗口为 60.0 FPS、平均渲染约 11.07–12.04 ms；23/23 源序列串口入口通过。
- 大尾巴提供真机照片并报告黑体白眼已显示；单张抓拍尚不能证明所有动画与网页逐帧一致，外设和 30 分钟稳定性仍待验收。

## v0.2.0 - 2026-09-16（首次写入完成，等待真机功能验收）

### 新增

- 选定 `KK StopWatch Avatar` 作为 Grok 风格圆屏表情机器人的工程底座，并锁定上游提交。
- 新增 v0.2.0 详细开发 PRD，明确产品范围、技术架构、目录规划、任务拆分、性能指标、风险和验收门禁。
- 更新产品总览与当前版本计划，确定 PlatformIO/Arduino + M5Unified 技术路线。
- 记录个人、非商业、本地使用 Grok 参考资产的项目边界：允许直接复制和转换，不作为本版本开发阻塞项；工程按未来可开源维护，仍保留来源和资产隔离。
- 新增 Luna Max 目标模式开发任务书，将实施拆分为 L0—L7、G0—G3，并细化几何转换、文件责任区、测试、视觉评分、证据留存和停止条件。
- 新增 v0.2.0 实施进度与验收记录模板，供目标模式持续回填实际命令、退出码、哈希、截图、阻塞和 G3 报告。
- 导入 KK 锁定基线 `204963257cb4dc2f3d7501eff900897bac55ef82`，建立 `kk-grok-v0.2.0` 本地开发分支和来源记录。
- 新增 Grok 几何源数据层、确定性 Python 转换器、固定点 C++ 几何、8 组实现眼睛映射和 `GrokRenderer` 固件渲染器。
- 完成 12 状态 SVG/HTML 桌面预览、3 组关键帧对照、浏览器实视证据和每状态视觉评分。
- 完成项目隔离 PlatformIO Core `6.1.18`、原版 KK 基线构建、最终清理后构建、测试、不变量审计和 v0.2.0 固件产物。
- 在收到大尾巴明确授权后，重新识别 `COM5`、ESP32-S3 rev `v0.2`、MAC `28:84:85:44:6C:00` 和 16MB Flash；完成首次设备写入。
- 写入 bootloader、既定 `default_16MB.csv` 分区表、`boot_app0` 和 v0.2.0 应用；四段写后 `verify_flash` 均返回 `verify OK (digest matched)`。

### 验证与边界

- 已核对 KK 上游工程配置、依赖提交、功能说明和 GitHub Actions 成功记录。
- 已核对 Grok Icon Study 的几何数据规模与锁定提交。
- 原版和 Grok 固件均已在本机完成构建验证；最终 Flash map 使用量为 `906229` bytes，RAM 为 `29888` bytes。
- 已生成固件、ELF、MAP、bootloader、分区镜像、SHA-256、测试日志、视觉证据、G3 预检和 G4 写入报告；源码工作树尚未提交。
- 未执行整片擦除、eFuse、安全启动或 Flash 加密；写入过程中仅擦除了四个目标镜像覆盖的 Flash 扇区，这是 `write_flash` 的必要操作。
- 真机屏幕、触摸、按键、振动、IMU、串口性能和 30 分钟稳定性尚未完成；串口应用启动日志受 USB CDC DTR/RTS 控制线影响，未作为通过证据。
- 根项目当前未初始化 Git；正式源码仅保留上游锁定提交和未提交工作树状态，本次未创建本地归档提交。

## v0.1.2 - 2026-09-16

### 新增

- 新增当前 StopWatch 的 16MB 完整 SPI Flash 原始镜像。
- 新增设备信息、SHA-256 校验文件、恢复说明和带人工口令保护的恢复脚本。
- 在 `C:\Users\zgzzm\Documents\M5Stack-Backups\` 建立第二份本机副本。

### 验证

- 只读识别设备为 ESP32-S3 rev v0.2、16MB Flash、8MB PSRAM。
- 完整读取 `0x000000–0xFFFFFF`，镜像大小为 `16777216` 字节。
- 镜像 SHA-256：`ACC86A969D2C201564A0910654261381A7A7D9F3D579A692908B24799626B87B`。
- esptool `verify_flash` 返回 `verify OK (digest matched)`。
- 项目副本与 Documents 第二副本的 SHA-256 完全一致。
- 未擦除或写入 Flash，未读取或修改 eFuse。

## v0.1.1 - 2026-09-16

### 新增

- 从 M5Stack 官方 CDN 获取并安装 M5Burner v3.0 到当前用户程序目录。
- 新增开始菜单快捷方式和 M5Burner 烧录准备笔记。
- 补充 UIFlow2 Web、StopWatch UIFlow2 和 M5Stack 下载页索引。

### 验证

- 校验下载包 SHA-256：`818A4983C87BF58F1ABAB077DB3F5BB3EC0C279468648300CEF9362FA9D2C69B`。
- 验证 ZIP 无绝对路径或 `..` 路径穿越项；Windows Defender 自定义扫描未发现相关威胁。
- 验证 M5Burner 正常启动，文件版本为 `3.0.0`，在线界面版本为 `v202605221800`，并可见 `STOPWATCH` 分类。
- 复核目标设备仍枚举为 `COM5`、`VID_303A&PID_1001`、状态 `Started`。
- 本版本未下载目标固件、未擦除 Flash、未执行烧录。

## v0.1.0 - 2026-09-16

### 新增

- 初始化轻量嵌入式软件开发项目结构。
- 新增 README、项目 AGENTS、产品总览 PRD 和当前版本计划。
- 新增 StopWatch 硬件与开发知识基线及官方资料索引。
- 新增源码、输出、素材、临时文件和工具目录说明。

### 验证

- 确认项目初始化前目录为空，不存在需保留的用户文件。
- 复核 Windows 当前可见的 ESP32-S3 USB JTAG/串口设备及 `COM5` 状态。
- 本版本仅创建项目文件，未向硬件写入任何内容。
