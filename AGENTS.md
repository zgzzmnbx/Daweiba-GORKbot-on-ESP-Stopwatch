# M5Stack StopWatch 项目规则

## 1. 项目定位

- 项目类型：轻量嵌入式软件开发项目
- 项目目标：以现有机器人工作台和固件为基础，建设 Gork 机器人控制台，统一桌面角色、语音交互和 StopWatch 设备；保留可复现固件工程。
- 最终交付物：源码、构建说明、Grok 风格几何资产、固件产物、测试记录、恢复说明和使用说明。
- 当前阶段：v0.8.0-dev 已完成控制台、服务托管/回答适配、统一延迟音频、本机打包和自动回归；v0.8.4-dev 完整初始化 ES8311 后已真机出声，v0.8.5-dev 将内置声音 PCM 幅度提高 3 倍但用户确认 100% 仍不够大。v0.8.6-dev 仅对六个内置短音再增加 ES8311 `+9.5 dB` codec 增益，Settings 首页显示版本号；已完成回归/构建、仅写入应用区并独立读回通过，用户真机确认声音达到正常大小。版本文字实屏、六种声音逐项音质及其余人工 A1–A10 门禁保留。
- 当前固件源码与设备 app0 均为 v0.9.0-dev；控制台源码已定版 v0.13.0，8766 运行后端仍为 v0.13.0-dev，语音模块 v0.3.2 已迁入。Opus 对同一 5.592 秒 WAV 的 Watch 发送阶段为 3.66 秒，PCM 为 32.22 秒；正式对话页 Opus 短句获用户完整可闻、主观音质可接受的听验。Electron 可见窗口尚未重开，真实掉线恢复、长稳及陌生端拒绝仍待验收。写入校验与 `played` 回执自身不等于可闻验收。
- 当前视觉目标：Bible Strong Avatar Lab 的 `Grok bot`；v0.2.1 方案见 `00-docs/00-PRD/06-v0.2.1-Grok-bot视觉对齐方案-【codex】.md`。v0.2.0 的 Grok Icon Study 仅是历史实现来源，不再作为最终视觉验收基准。

## 2. 当前任务边界

- 2026-09-24 v0.13.0 / 固件 v0.9.0：本次授权范围内仅写当前 Watch app0 并独立执行 `verify_flash`，原 PCM 客户端兼容、新客户端 Opus 协商通过。正式后端已恢复 BLE 自动连接；可闻听验与其余安全门禁见 v0.13.0 实施记录。后续新镜像写入仍须当次授权。

- 2026-09-24 v0.12.9 / 固件 v0.8.8：电脑端最多 3 包在途；按大尾巴当次授权仅写入 app0 并独立校验。正式实例在普通小人页传输并播放完整短句，用户确认清楚完整；约 89.6 KB 的传输需 17.5 秒，含播放约 18.5 秒，仍需优化。录音限 Audio test；其余真机门禁见 v0.12.9 实施记录。再次写入新镜像仍须当次授权。

- 2026-09-24 v0.12.8：状态日志增加右侧展开标签页、筛选/复制/跟随，左侧增大正文；仅前端内存摘要，未新增连接、未重启正式实例。

- 2026-09-24 v0.12.7：Watch 朗读必须进入设备 Settings → Audio test；一次正式任务在音频建立阶段 0/69120 字节失败，旧前端却误报播放完成。电脑端已修复终态判定、空错误说明及命令超时提示；正式进程未重启，固件未改、未烧录，设备播放和听感待验。

- 2026-09-24 v0.12.6：对话页统一文字输入，分选电脑朗读、小人显示（Watch/桌面/两端）与 Watch 朗读实验；原文/AI 回答独立选择。移除角色气泡及 Watch 文字朗读的重复编辑框，保留清除与设备录音实验。源码 v0.12.6-dev，正式实例未重启；Watch 实屏和出声未验收，固件仍 v0.8.7-dev。

- 2026-09-24 v0.12.5：桌面小人“始终置顶”可切换并持久化；源码 v0.12.5-dev，正式 Electron 实屏与重启恢复待验收。默认延续旧版置顶。仅电脑端窗口/控制台改动，固件仍 v0.8.7-dev。

- 2026-09-24 v0.12.4：仅电脑端状态终端与双栏布局补丁，源码 v0.12.4-dev，正式实例未重启；设备仍 v0.8.7-dev。终端只读，最多120条内存摘要，不采集聊天正文、原始异常或凭据；不为日志新建 BLE/语音连接或轮询。保留黑白主题选择、清空和内部滚动。源码版本与运行版本分开记录。

- v0.12.3 最新用户要求：这台电脑与此 Watch 一对一使用，由 Gork 在程序内配对，持续自动重连，无须预先进入 Windows 蓝牙设置。当前本机忽略配置已固定目标地址 `28:84:85:44:6C:01`。隔离预览必须 auto_connect_device=false；手动断开取消自动重连。

- 2026-09-23 最新用户授权：将语音服务源码、模型及运行环境迁入本项目，取消本机跨项目运行依赖；见 PRD 20。此条覆盖下方历史“不搬模型/venv/Key”等限制中的源码、环境、模型部分。凭据继续使用 Windows 专用环境变量，不写入仓库；来源目录保留，HTTP 模块边界保留，固件不改。

- 2026-09-23 UI 原型追加要求：机器人下方显示语音/设备/朗读状态与自动朗读、表情联动、桌面小人开关；默认云端朗读、本地识别。正式实施须保留旧保存偏好与许可撤销，默认不触发自动上传；当前仅 v1.0.3 原型与 PRD，生产设置未改。

- 2026-09-23 后续 UI 最新方向：PRD 19 / 原型 v1.0.3，当前源码 v0.10.0-dev。用户看过 v1.0.2 后明确改回“左侧常驻机器人 + 右侧对话/角色/StopWatch/设置标签页”，约 4:6；不使用顶部头像或侧滑面板，切换仅影响右侧内容，机器人持续动画。窄屏可收成紧凑伙伴区。用户已批准原型，正式界面由 Astra 落地为 v0.10.0-dev，Sol 负责回归；当前结论见 04-output/v0.10.0/。PRD 18 模块边界保留；运行实例、发行包及真实设备验收不随源码结论更新。

- 最新范围优先（2026-09-23）：v0.9.0 已按 PRD 16 与 Goal 任务书 17 完成电脑端源码、自动回归和隔离网页检查，将控制台整理为“对话 / 角色 / StopWatch / 设置”，并补齐必要角色控制、工作台控制权解耦和全局停止。电脑端源码为 v0.9.0-dev，独立语音服务仍 v0.3.2，设备仍 v0.8.6-dev；正式 Electron、云端和设备人工验收未包含在本次源码结论中。
- v0.9.0 不改外部语音仓库、不搬模型/venv/Key、不改固件/烧录，不优化实时 BLE，不更新旧发行包。真实设备、云请求、正式实例重启按任务书门禁处理；历史 PRD 13/14 为已实施基线，原有安全边界继续生效。
- 用户明确要求语音能力模块化复用（2026-09-23）：补充 18 是本轮强制范围。独立 ASR/TTS 服务通过版本化 HTTP 提供能力，客户端录音/播放从 Gork 页面提取，HTTP 客户端无 BLE/角色/模型依赖；交付独立调用示例及 V01–V08。当前服务单活会话，不可假称多程序任意并发；不为模块化另建引擎副本。
- 合并指产品入口和生命周期，不授权搬迁外部语音仓库、模型、venv、Key 或合并 Git 历史；需要外部接口变更时另行交接。设备烧录仍需当次明确授权。

- 2026-09-21 新路线优先：用户已批准开发纯 BLE 设备音频。v0.7.0-P0 测试版已构建，详见 PRD 11 与 `12-v0.7.0-P0音频探针协议与操作-【codex】.md`；不引入 Wi-Fi/标准蓝牙耳机。必须先 G1 烧录授权、P1 录放与 P2 吞吐/安全实测，再推进 P3 语音集成；当前禁止宣称全部功能完成。

- 本项目主要处理：StopWatch 硬件认知、开发环境、固件工程、外设驱动、UI交互、测试和发布产物。
- 当前暂不处理：账号系统、量产方案、云端服务部署、公开发布和外部消息集成。设备音频按 v0.7.0 分阶段推进。电脑端可经独立本机语音服务使用已明确授权的云端 ASR/TTS，不在本仓库保存凭据。
- 不要改动：用户未授权的其他项目、全局配置、出厂固件和芯片永久安全配置。
- 需要先确认再做：首次烧录、擦除 Flash、修改分区表、eFuse、安全启动、Flash 加密、外部电池接线和任何不可逆操作。

## 3. 启动澄清规则

- 本项目是否已完成启动澄清：v0.2.0 范围已完成。
- 启动澄清记录位置：`README.md` 的“项目启动摘要”。
- 已确认：目标硬件、Grok Avatar 应用、个人使用场景、技术栈、工程底座、视觉方向、首轮验收方式和版本规则。
- 尚待确认：实物硬件版本、首次烧录时点、长期功耗目标和联网语音版本口径。
- 尚未澄清的内容不得被擅自固化为最终产品需求。

## 4. 协作与状态维护

- 当前权威 PRD：`00-docs/00-PRD/16-v0.9.0-Gork控制台界面重组PRD-【codex】.md`；执行包：`00-docs/00-PRD/17-v0.9.0-Goal模式执行任务书-【codex】.md`（文档均 v1.0.0）。源码交付和人工门禁见 `04-output/v0.9.0/实施与验收记录-【codex】.md`。历史基线为 PRD 13 与任务书 14。

- 是否允许多个 Codex 线程协作：否；如需并行，先划分不重叠文件责任区。
- 当前版本计划：`00-docs/00-PRD/02-当前版本计划.md`
- Luna Max 目标模式任务书：`00-docs/00-PRD/04-Luna-Max目标模式开发任务书-【codex】.md`
- 实施进度与验收记录：`00-docs/00-PRD/05-v0.2.0实施进度与验收记录-【codex】.md`
- v0.2.1 视觉对齐方案：`00-docs/00-PRD/06-v0.2.1-Grok-bot视觉对齐方案-【codex】.md`
- v0.3.0 BLE 表情操控 PRD 与 Luna Max 目标模式交接：`00-docs/00-PRD/07-v0.3.0-BLE表情操控开发PRD-【codex】.md`
- v0.4.0 派生表情需求与验收：`00-docs/00-PRD/08-v0.4.0-happy-work派生表情-【codex】.md`
- v0.4.0 本机构建与待真机验收记录：`04-output/v0.4.0/实施与验收记录-【codex】.md`
- v0.5.0 BLE 聊天气泡 PRD 与验收：`00-docs/00-PRD/09-v0.5.0-BLE文字聊天气泡-【codex】.md`、`04-output/v0.5.0/实施与验收记录-【codex】.md`
- v0.6.0 电脑语音工作台：`03-Src/stopwatch-voice-companion/README.md`、`00-docs/00-PRD/10-v0.6.0-电脑语音工作台-【codex】.md`、`04-output/v0.6.0/实施与验收记录-【codex】.md`
- 当前状态：`README.md`
- 版本记录：`CHANGELOG.md`
- 新线程开始前必须阅读：`AGENTS.md`、`README.md`、`CHANGELOG.md`、`00-docs/00-PRD/02-当前版本计划.md`；Luna Max 实施线程还必须阅读详细 PRD、目标模式任务书和实施进度表。
- 需求边界、任务状态或验收口径变化时，更新当前版本计划；阶段结论同步到 README 和 CHANGELOG。

## 5. 目录规则

- `00-docs/00-PRD/`：产品与版本计划，不放编译缓存。
- `01-assets/design/`：设计稿和视觉资产。
- `01-assets/bug/`：故障证据。
- `01-assets/reference/`：官方资料索引和参考文件；外部资料需记录来源与获取日期。
- `02-notes/`：知识基线、调研、实验笔记；必须区分官方事实、本机观察和推断。
- `03-Src/`：唯一正式源码区；选定技术栈前不要并列创建多套互相冲突的工程。
- `04-output/`：可交付的固件、发布包和报告。
- `Codex-Temp/`：构建缓存、下载文件、串口日志、截图和一次性输出。
- `tools/`：维护和验证脚本。

## 6. 版本规则

- 固件源码及设备 app0 v0.9.0-dev、控制台源码 v0.13.0、8766 运行后端 v0.13.0-dev、音频测试工具 v0.9.0 分别记版本，不把构建、写入校验或 `played` 回执当可闻验收。
- 第一位版本号仅由大尾巴明确决定；阶段功能更新第二位，小修更新第三位。
- `CHANGELOG.md` 最多保留 5 个版本，超出部分转入 `CHANGELOG-历史归档.md`。
- 固件和交付物文件名必须包含版本号；Codex 新建的独立交付文档默认追加 `-【codex】`。
- 第二位版本号变化且项目已启用 Git 时，默认进行一次本地 Git 存档；不自动推送远端。

## 7. 开发要求

- 选定技术栈：PlatformIO Core `6.1.18` + Arduino + M5Unified/M5GFX/M5PM1/M5IOE1；版本和提交以 README 与 v0.2.0 PRD 为准。
- 工程底座锁定 `Trentct/m5stack-stopwatch-avatar@204963257cb4dc2f3d7501eff900897bac55ef82`；Grok 几何参考锁定 `blessonism/grok-icon-study@647e9bd7c60290c42a738fad586589b3f36a4680`。
- v0.2.1 的视觉与行为输入改为 `smontlouis/bible-strong-avatar-lab@79fe9ba06e4874b11394b8e8a3f2c493c9d197ba` 的 `Grok bot`；保留 KK 硬件底座，不直接将浏览器渲染运行时移植到 MCU。
- v0.2.2 播放规则：仅 `idle` 默认持续；其他 22 条序列默认播放一遍后回 `idle`。串口显式 `loop` / `pingpong` 可持续播放；`once` 播完也回 `idle`。不得把构建通过说成设备已更新。
- v0.2.3 将原 A+B 长按诊断页作为设置页，新增亮度 `−/+` 触控（30–255，步长 15，NVS 持久化），保留硬件检查；写入和真机验收状态见 `04-output/v0.2.3/亮度设置实施与验收记录-【codex】.md`。
- v0.2.4 设置首页增加 M5PM1 电池电压估算百分比与可持久化调试开关；调试开关仅在表情画面底部显示小白字动画名。电量是估算值，不能称为精确 mAh 容量；真机验证边界见 `04-output/v0.2.4/电池与调试模式实施验收-【codex】.md`。
- v0.2.5 仅将调试模式的小白字动画名移到黑色圆体顶部，其余表情、设置与电量逻辑不变。
- v0.2.6 在 `tools/` 增加可双击的 Windows 表情串口控制台；自动识别当次 StopWatch 端口，避免硬编码 COM5；该版本未改动或重新烧录固件。
- v0.2.7 沉淀 v0.3.0 BLE GATT 表情控制需求和 Luna Max 阶段门禁；v0.3.0 已在正式源码建立 BLE 服务、加密绑定、Bluetooth 设置页和 Windows Bleak 控制台，并已仅写入应用区。当前已完成本机配对和核心控制，但 G4 仍必须实测陌生端拒绝，不可退化为开放写入。
- v0.4.0 新增固件本地派生 `happy-work`：完全复用 `happy` 来源眼睛时间轴，下半部独立程序化纸笔/手部书写层；23 条来源序列保持不变，24 个可调用表情。已随 v0.5.0 写入，真机视觉与帧率待验收。
- v0.5.0 在原加密 BLE GATT 命令特征上增加文字分包（每包 ≤20 字节、整条 ≤72 UTF-8 字节/24 字符），仅主循环完成重组和气泡绘制。已写入并读回；中文 glyph、清屏和帧率仍须真机验收。v0.6.0 客户端不再将旧状态读取当新回执，工作台增加最多 2 次重连，但不能宣称旧固件协议已增加逐命令事务编号。
- v0.6.0 电脑端独立 Python 环境、同源网页和 HTTP 语音适配；旧 BLE 入口保留。Windows 音频由浏览器负责，完整 WAV 才能播放；上传许可默认双关、撤销立即取消，停止先本地静音再取消服务；旧 generation 的结果不能触发音频或新 BLE 状态。
- 大尾巴导出的 `01-assets/design/Gorkbot-avatars.bible-strong.app/avatar-studio-project.json` 已作为 v0.2.1 正式数据快照；转换脚本和构建不依赖 `Codex-Temp` 中的临时克隆。再次写入前重新枚举串口并核对 MAC，绝不能因为历史记录是 `COM5` 就写入当前 COM3/COM4。
- 实现顺序必须是：导入并保留上游来源 → 原样编译通过 → 再建立独立资产层和动画改造；不要一开始重写底层驱动。
- 保留上游 dirty-rectangle 局部刷新和状态机设计；性能退化必须有测量证据，不能凭观感判断。
- 使用项目相对路径，不把本机绝对路径或固定 COM 口写死到代码。
- 端口、芯片和 Flash 参数应在运行前探测或通过配置显式提供。
- 硬件驱动优先使用 M5Stack 官方库和官方示例；第三方依赖必须记录版本与来源。
- 屏幕、触摸、音频、电源管理和 IO 扩展互相耦合，不能把复位/供电控制当作普通直连 GPIO。
- 修改代码必须配套最小验证；能编译就编译，能做硬件测试就记录实测结果。
- Grok 参考资产按大尾巴确认的个人、非商业、本地使用范围直接复制和转换，不把第三方资产限制作为本版本开发阻塞项；仍须记录来源及提交号，并将资产与通用引擎代码分层。v0.2.0 不执行公开发布。

## 8. 硬件安全边界

- StopWatch v1.0 部分批次贴纸的 `BAT` 实际是 `5V IN`；硬件版本未确认前禁止连接外部锂电池。
- v1.0.1 标记为 `*BAT` 的位置才是电池引脚。任何接线前再次对照官方原理图和实物版本。
- 未经明确授权，不读取或修改 eFuse，不启用安全启动或 Flash 加密，不执行整片擦除。
- 首次烧录前确认官方恢复固件或恢复路径；烧录后记录固件版本、命令、端口和验证结果。常规迭代不再重复计算或核验文件 SHA-256，也不为新版本强制生成 `SHA256SUMS.txt`；保留烧录工具自身的写入校验和必要的设备读回验证。整片恢复仍须单独确认并遵循恢复说明。
- 当前 `COM5` 仅为 2026-09-16 的本机快照，后续必须重新枚举。
- M5Burner 安装于当前用户目录 `C:\Users\zgzzm\AppData\Local\Programs\M5Burner`；烧录前仍需重新确认端口、固件型号和版本，不以界面默认值代替检查。

## 9. 验证方式

- 文档验证：UTF-8 可读、关键链接存在、规格和本机观察来源明确。
- 开发环境验证：工具版本、板卡包版本、依赖锁定和端口枚举可复现。
- 编译验证：从干净环境完成构建并保存命令、版本和结果。
- 硬件验证：按屏幕、触摸、按键、振动、音频、IMU、RTC、Wi-Fi、电源和扩展接口分项记录。
- 交付前检查：源码、固件、构建说明、测试结果、已知问题、版本号和 CHANGELOG 一致。

## 10. 项目经验 / 注意事项

- 真机朗读听验必须用完整语句的 WAV；从较长语音中间截取的短样本只能测传输，末尾缺字属于音源截断，不能据此判定 Watch 播放缺字。`played` 仍需用户实际听验确认。

- Watch 文字气泡与 BLE 音频是独立通道；文字显示不能证明音频到达。v0.8.8 小人页 PCM 播放已获用户听验；v0.9.0 同音频 Opus/PCM 均返回 `played`，正式入口的 Opus 短句也获用户完整可闻确认。音频进度只按设备确认的字节推进，不能按电脑本地写入返回或 `PLAYING` 推断可闻。

- Windows 显示 StopWatch“已配对”不能证明加密 GATT 可用。若 WinRT 无缓存查询全部服务 `Unreachable`、缓存查询 `access denied`，先核对双端旧绑定是否失配；本机 v0.8.7 实测通过手表清绑定并由 Bleak 程序化 `unpair()`、`pair=True` 重新配对恢复服务发现，无须在 Windows 设置里手动配对。手表停在 Settings 时表情命令返回 `ERR:BUSY` 是预期行为，验收回执须返回机器人画面。

- BLE 绑定后的 Windows 端可能用可解析私有地址重连；固件不要在加密认证完成前将原始连接地址与已保存身份比较并断开。认证后再核对绑定身份，未认证连接不可写入。WinRT 报 GATT `Unreachable` 时先检查此顺序及服务发现，不把系统“已配对/未连接”当作连接成功或配对丢失的单独证据。

- Windows 托盘图标使用 NativeImage 位图/PNG 表示并检查 isEmpty；不要传 SVG 数据地址。图标调整需要重启 Electron，网页刷新无效。

- 控制台控件遵循 PRD 21 界面设计规范及 style.css 间距变量；输入/按钮/辅助说明必须分层留白，检查聚焦和窄屏状态，不能只检查默认空态。

- 2026-09-23 v0.11.0：语音服务已迁入 `03-Src/voice-service/`，环境/模型使用 `Codex-Temp/voice-runtime/`；不再从旧语音项目查找运行文件。默认托管显式指定本模块配置，避免继承旧 VOICE_CONFIG。云 Key 继续指定 Windows 环境变量，不存文件。新的 venv 用本机相同基础 Python 创建，复制依赖库后必须检查 import/pip check/真实模型加载，不能沿用旧 Scripts 内嵌绝对路径启动器。


- 2026-09-23 电脑端 v0.8.8-dev 与语音服务 v0.3.2 联动：音色目录以 `/v1/capabilities` 为准，云端 `cloud_voice` 逐请求传递；旧服务未声明 `request_voice` 时不发送新字段。托管入口是独立语音仓库 `tools/serve_managed.py`，本机路径只存忽略的 config.local.toml，不复制模型、venv 或密钥。重试只启动/复用，不强制重启外部实例。

- 2026-09-23 用户明确要求三项上传许可默认同意：电脑端 v0.8.7-dev 默认勾选并在连接时应用，保留独立撤销且记住撤销；不因默认授权自动录音或切云端。此条优先于旧 PRD 的默认关闭描述；服务端无前端授权请求时仍默认关闭。
- 桌面/网页角色必须共用 `static/gork-avatar.js` 和 Watch 正式快照导出的 catalog。运行 `tools/export_desktop_avatar.py --check` 检查桌面分发副本；不能再用独立 CSS 眼睛冒充来源动画。电脑源码 v0.8.7-dev，设备仍 v0.8.6-dev，旧打包目录需另行更新。

- 协议 `STARTED`/`PLAYING` 及播放结束都不是实际可闻证据。v0.8.2 本地 Test 显示 `playing` 仍无声，证明驱动启动、G3/G10 锁存、ES8311 I2C 和 PCM 入队通过，但不能证明 codec/I2S/功放/扬声器有真实输出。
- StopWatch 的 M5IOE1 同时承载振动 PWM、codec 电源 G3 和功放 G10；应用不得再创建第二套 M5IOE1 驱动实例。M5Stack 官方出厂示例还同时控制 ESP32 GPIO14 作为扬声器功放门控；播放必须在 codec/G10 就绪后拉高 GPIO14，停止时先拉低 GPIO14，再结束 M5Unified 扬声器。
- M5Unified StopWatch 回调只写少量 ES8311 寄存器，不能等同官方 `esp_codec_dev 1.5.4` 初始化。v0.8.3 双 PA 已开启仍无声；v0.8.4 完整配置并读回 44.1 kHz/16-bit I2S、DAC 参考和解除静音后已真机出声，确认初始化不完整是根因。100% 音量仍偏小，后续需在避免失真的前提下分别核对 PCM 峰值、软件增益和 codec 输出增益。
- v0.8.4 六个内置声音 PCM16 峰值仅 10,000，存在 3.2767 倍无数字削波余量。v0.8.5 仅将内置声音峰值提高至 30,000（3 倍、约 +9.54 dB），保持 codec 0 dB，避免同时放大动态语音和录音回放；无数字削波仍不能替代真机失真、爆音和发热听验。
- v0.8.5 的 PCM 峰值 30,000 已接近 16-bit 上限，不能再直接乘 3。v0.8.6 将 ES8311 REG32 从 `0xBF` 提高到 `0xD2`（+9.5 dB）且仅限内置短音；这是名义电压增益，不保证声压或听感严格 3 倍，高音量可能削波/饱和。真机必须从 20%–30% 分级上调，达到够用响度即停止；动态 PCM/录音回放保持 0 dB。
- v0.8.6 写入后用户确认声音已达到正常大小，后续不得继续叠加 PCM 或 codec 增益；本次只关闭最大响度不足问题，不替代六种声音逐项失真、底噪、异常发热与长期稳定性验收。

- Electron 启动器不得用阻塞的 `npm start` 保留命令窗；重复打开时由单实例事件唤醒现有控制台，未取得锁的进程不得进入 IPC、窗口或后端初始化，避免并发触碰 Chromium/GPU 缓存产生 `0x5`。
- v0.8.0 控制台只允许一个 BleakClient 同时承载表情、声音和音频服务；Windows 实机连接使用三个指定 UUID 的 UNCACHED 发现。模拟测试通过不代表本机三服务发现已通过，烧录 v0.8.0 后必须回归，失败时不要自动清绑定或重刷。
- Windows x64 目录包自带当前本机 Python venv 和 Electron，但独立语音引擎、模型与凭据不在包内；只能称本机可启动包，不能称跨电脑完全便携。

- 音频驱动 begin()/入队会推进时钟，不能用循环入口的旧 now 减新 blockStarted_ 做无符号超时判定；采集分支重新取时并覆盖旧时钟/边界/回绕编译断言。错误提示须保留到新操作，不被退出 CANCEL/断连覆盖。

- v0.7.1 电脑音频测试工具必须使用指定音频 service UUID + `winrt={"use_cached_services": False}`；本机默认枚举曾只见旧表情服务，全服务 UNCACHED 查询报 0x8000FFFF，指定音频服务 UNCACHED 实测可发现新特征。发现缺失不能断言固件旧版，也不要先清绑定/重刷。设备固件仍 v0.7.0-P0。

- 音频测试入口为 `start-ble-audio-test.cmd`，与 8766 工作台/旧控制台互斥连接设备；P0 不访问语音服务。测试录音仅显式保存到用户指定新文件，优先 `Codex-Temp/`，禁止把私人 WAV 加入 Git。
- StopWatch 官方 Mic/Speaker 共用 codec 电源与时钟，必须半双工并在释放 PCM 前 `end()` 等待任务退出。Arduino BLE `notify()` 会遍历所有连接；敏感音频须通过定向通知发送给当次已授权连接，不能只给 CCCD 加密便认定安全。

- 当前开发前恢复点：`04-output/backups/2026-09-20-before-voice-companion-v0.5.0/`；两个独立 16 MB 读取副本逐字节一致。本地保存，不公开；不增加常规 SHA 文件校验。
- BLE 表情/清屏回执无 request_id：客户端必须单写入者、匹配回执、超时断开，不能用读到的上次状态冒充新请求成功；重连不重放旧文字。电脑端与语音服务 session/turn/generation 三层边界分别处理。

- USB VID/PID `303A:1001` 和 `USB JTAG/serial debug unit` 能证明存在 ESP32-S3 原生 USB 接口，但不能单独证明板型、硬件版本或下载模式。
- 官方 Arduino 路线要求选择 `M5StopWatch` 板型并安装 M5Unified、M5GFX 及依赖；官方出厂演示工程当前说明使用 ESP-IDF 5.5.4。
- StopWatch 的 M5IOE1 负责屏幕/触摸复位、音频供电、扬声器、振动及 USB/UART 复用等关键控制，初始化顺序必须参考官方实现。
- M5Burner 官方 Windows 包的主程序当前无 Authenticode 签名；安装或升级时保留官方来源 URL、SHA-256 和安全扫描结果，不从第三方镜像获取。
- 首次烧录前的完整 Flash 恢复点位于 `04-output/backups/2026-09-16-StopWatch-current-state-v0.1.1/`；恢复前必须校验镜像 SHA-256、目标 MAC 和实际端口，并再次取得整片写入确认。
- v0.2.0 新线程可自行完成源码导入、工具链安装、静态检查和本地构建；到 G3 首次烧录门禁必须停止，未经大尾巴当次明确确认不得写入设备。
- 本机 Python 3.14 的 Windows `platform.system()`/`platform.platform()` 在 PlatformIO 初始化期间可能阻塞；构建使用项目内兼容包装和隔离 Core，不把该宿主机补丁写入正式源码。
- 本机深层同步/临时路径可能触发 Xtensa 编译器 `CreateProcess` 失败；构建时可将正式源码和 PIO home 映射到临时盘符缩短路径，盘符和绝对路径不得写入源码或配置。
- ESP32-S3 USB-Serial/JTAG 打开 COM 口时默认 DTR/RTS 可能把设备带入 ROM `DOWNLOAD`；串口空白或启动日志异常必须结合显式控制线、写后 `verify_flash` 和人工屏幕观察判断，不能单独宣称应用失败或成功。
- 2026-09-17 实测打开 COM5 观察 v0.3.0 时出现 `USB_UART_CHIP_RESET`/`DOWNLOAD`；串口监视会改变 USB-Serial/JTAG 状态，BLE 真机验收期间不要把该日志当应用启动结论，必要时关闭串口并用 esptool 硬复位或物理重启后再观察。
- PlatformIO `write_flash` 会擦除所写镜像覆盖的 Flash 扇区；记录烧录结果时要与整片 `erase_flash` 分开表述，并明确列出实际写入偏移和读回校验。

## 交接后的开发分工（2026-09-20，优先于此前阶段安排）

- StopWatch 仓库负责机器人交互和电脑端连接程序；语音仓库只维护独立服务与接口。首轮不烧录、不实现设备自身音频。
- 本轮交接文件在 `00-docs/02-交接/电脑语音交互首轮任务书-v1.0.0-【codex】.md`；文档版本 v1.0.0。两个仓库代码不互相复制，不共享模型/venv/Key。
- 会话占用、独立上传许可、完整 WAV、停止播放与服务取消的区别必须按交接契约实现；不能偷偷接管会话或默认开上传。
